# Технический Гайд: Firebase и Деплой 🚀

## 🌐 Firebase Console

Управление проектом происходит здесь: **[Firebase Console: lily-next](https://console.firebase.google.com/project/lily-next/overview)**

### 1. База Данных (Firestore Database)

Здесь хранятся все тексты, цены и связи.

- **Коллекция `categories`**:
  - `name`: Название (напр. "Букеты")
  - `emoji`: Иконка
  - `order`: Число для сортировки (меняется через Drag & Drop)
- **Коллекция `products`**:
  - `name`, `price`, `description`
  - `imageUrl`: Ссылка на картинку
  - `categoryId`: ID категории (или строка `'undefined'`, если категория удалена)

### 2. Хранилище Файлов (Storage)

Здесь лежат загруженные картинки товаров.

- Папка `products/`: Все изображения попадают сюда.
- **Важно**: В `next.config.ts` прописан домен `firebasestorage.googleapis.com`, чтобы Next.js разрешал показывать эти картинки.

---

## 🚀 Как задеплоить обновления

Если вы внесли изменения в код и хотите обновить сайт в интернете:

### Шаг 1: Сборка проекта

Next.js должен превратить ваш код в статические HTML/CSS/JS файлы.
Откройте терминал в папке `next-site` и введите:

```powershell
npm run build
```

_Если всё ок, появится папка `out`._

### Шаг 2: Отправка в облако

Используем Firebase CLI для загрузки папки `out` на хостинг.

```powershell
npx firebase-tools deploy --only hosting
```

### Результат

Сайт обновится по адресу: **https://lily-next.web.app**

---

## ⚠️ Возможные проблемы

1.  **Картинки не грузятся**:

    - Проверьте `next.config.ts` — там должен быть разрешен домен источника картинок.
    - Проверьте правила Storage в консоли Firebase (должен быть разрешен read/write).

2.  **Симлинки и модули**:

    - Проект использует локальные `node_modules`. Если что-то сломалось, попробуйте удалить `node_modules` и запустить `npm install`.

3.  **Ошибки при билде**:
    - Команда `npm run build` очень строгая. Она проверяет типы TypeScript и ошибки линтера. Если билд падает, читайте ошибку — скорее всего, где-то несовпадение типов.
