# 🌸 SweetBouquet - MVP Landing Page

## Что мы сделали

### ✅ Технологии

- **Next.js 16.0.3** (React 19, TypeScript, Turbopack)
- **Firebase Firestore** (проект: lily-next, база: lily-base)
- **Tailwind CSS** (для стилей)
- **Vercel** (готов к деплою)

### ✅ Структура проекта

```
src/
├── app/
│   ├── page.tsx              # Главная страница
│   └── globals.css           # Стили + анимация прокрутки
├── components/
│   ├── AppHeader.tsx         # Шапка сайта
│   ├── HeroSection.tsx       # Герой с CTA
│   ├── FeatureList.tsx       # 3 преимущества
│   ├── CategoryGallery.tsx   # Галерея категорий (grid 2-3 колонки)
│   ├── CategoryCard.tsx      # Карточка категории с hover-галереей
│   └── AppFooter.tsx         # Подвал
├── hooks/
│   ├── useCategories.ts      # Real-time категории из Firestore
│   └── useProducts.ts        # Real-time товары по категории
├── lib/
│   └── firebase.ts           # Firebase config (lily-base!)
├── types/
│   └── index.ts              # TypeScript интерфейсы
└── scripts/
    ├── seed-rest.js          # Пуш категорий через REST API
    ├── seed-products.js      # Пуш товаров через REST API
    └── update-images-null.js # Обнуление imageUrl для плейсхолдеров
```

### ✅ Firebase структура

**База данных: lily-base** (не default!)

#### Коллекция `categories`:

```javascript
{
  id: 'клубника',
  name: 'Клубничные букеты',
  description: 'Сочная клубника с цветами',
  emoji: '🍓',
  order: 1,
  createdAt: timestamp
}
```

**5 категорий:**

- 🍓 Клубничные букеты
- 🍫 Шоколадные композиции
- 🥭 Экзотические букеты
- 🫐 Ягодные композиции
- ✨ Премиум коллекция

#### Коллекция `products`:

```javascript
{
  id: 'strawberry-classic',
  categoryId: 'клубника',
  name: 'Клубничная классика',
  description: 'Сочная клубника с белыми розами',
  price: 2990,
  imageUrl: '', // пока пусто = плейсхолдер
  inStock: true,
  order: 1,
  createdAt: timestamp
}
```

**20 товаров** (по 4 на каждую категорию)

---

## 🚀 Как запустить

### 1. Установить зависимости

```bash
cd next-site
npm install
```

### 2. Запустить dev-сервер

```bash
npm run dev
```

Откроется на `http://localhost:3000`

---

## 🔧 Как работать с данными

### Добавить категории

```bash
node scripts/seed-rest.js
```

### Добавить товары

```bash
node scripts/seed-products.js
```

### Обнулить картинки (показать плейсхолдеры)

```bash
node scripts/update-images-null.js
```

### Добавить реальные фотки

1. Залей фото на хостинг (Unsplash, Firebase Storage, Cloudinary)
2. Открой Firebase Console → Firestore → `products`
3. Найди нужный товар, измени `imageUrl` на URL фотки
4. Сайт автоматически обновится (real-time!)

---

## 🎨 Как работает hover-галерея

### Проблема (текущая)

- При наведении на карточку **должна** запускаться прокрутка товаров
- **НО:** прокрутка работает только при наведении на текст, не на картинку 😂
- Причина: анимация выталкивает товары за границы и мышка теряет hover

### Что нужно починить

```tsx
// CategoryCard.tsx
// Добавить pointer-events-none на анимируемые элементы
<div className="animate-scroll flex pointer-events-none">
```

Или переделать логику:

- Использовать CSS `:hover` вместо `onMouseEnter/Leave`
- Добавить `group-hover:` классы

---

## 📁 Важные файлы

### `.env.local` (не коммитится!)

```
NEXT_PUBLIC_FIREBASE_API_KEY=...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=lily-next.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=lily-next
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=lily-next.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=...
NEXT_PUBLIC_FIREBASE_APP_ID=...
NEXT_PUBLIC_FIREBASE_DATABASE_ID=lily-base
```

### `src/lib/firebase.ts`

**КРИТИЧНО:** Указываем базу `lily-base`!

```typescript
export const db = getFirestore(app, "lily-base");
```

---

## 🐛 Известные баги

1. **Hover-галерея глючит**

   - Прокрутка товаров работает только при наведении на текст
   - Нужно добавить `pointer-events-none` или переделать на CSS

2. **Все товары с плейсхолдерами**

   - `imageUrl` обнулён для демонстрации
   - Нужно залить реальные фотки в Firebase Storage

3. **Firestore индекс не создан**
   - Если будешь использовать `orderBy` + `where`, нужен composite index
   - Сейчас сортируем в коде (`data.sort()`)

---

## 🎯 Что дальше

### MVP v1.0

- [ ] Починить hover-галерею (pointer-events или CSS)
- [ ] Добавить реальные фотки товаров
- [ ] Настроить Firebase Storage для загрузки фото
- [ ] Создать страницу категории (`/category/[id]`)
- [ ] Создать страницу товара (`/product/[id]`)

### MVP v1.5

- [ ] Корзина (Firebase или localStorage)
- [ ] Форма заказа
- [ ] Интеграция с Telegram Bot для приёма заказов
- [ ] Админка для управления товарами

### Продакшн

- [ ] Деплой на Vercel
- [ ] Кастомный домен
- [ ] SEO оптимизация
- [ ] Аналитика (Google Analytics / Yandex Metrika)

---

## 📝 Заметки

- **Симлинки убили Nuxt**, поэтому перешли на Next.js
- **REST API надёжнее SDK** для пуша в Firestore (DNS не глючит)
- **База lily-base** (не default!) — важно везде указывать
- **Turbopack быстрый** — компиляция за 1-2 секунды
- **Плейсхолдеры крутые** — градиент + эмодзи + номер товара

---

## 🤡 Fun Facts

> "Пьяная Клауда создала компоненты, но забыла про Tailwind"

> "Hover работает только на тексте — это фича, не баг!"

> "Прокрутка товаров стоит на месте как памятник MVP"

---

**Создано с 💚 и хаосом в 3 часа ночи**
**Firebase: lily-next | Database: lily-base | Status: Working (mostly)**

<aside>
📑

## Final Project Report: SweetBouquet (MVP)

Статус: Code Complete (Waiting Deployment & Content)

Цель Спринта: Экстракция ресурсов (Funding Source).

### 🎯 Контекст (The Real Why)

Мать (Клиент) предложила проект, пытаясь откупиться от эмоциональной катастрофы.

Проект принят как расчетливый ход (Extraction Sprint). Поскольку извлечение из этих отношений жизни или любви невозможно, из них извлекаются деньги, чтобы обеспечить независимость Субъекта.

Стоимость: 10 000 руб. (Аванс на выживание и ресурсы для 2C).

### 🛠 Что сделано (Technical & Architecture)

**1. Фундамент и Стек**

- **Стек:** Next.js 16 (React, TS) + Tailwind CSS.
- **Архитектурный Принцип:** Полный отказ от хардкода. Сайт построен на компонентах (Atomic Design).
- **Database:** **Firebase Firestore** (lily-base). Успешно настроено соединение. Товары и категории загружаются в реальном времени из базы.
- **Значение:** Это не просто сайт. Это **Прототип** для архитектуры 2C (Блоки-Компоненты + Живая база).

**2. Архитектурные Решения**

- **Сепарация:** Данные (Firestore) полностью отделены от витрины (Next.js).
- **Admin Gate:** Разработана концепция скрытого входа **«Протокол Страус»** (Master Key Auth), чтобы передать Мать-Клиенту полный контроль над контентом (ценами/товарами), тем самым **устранив необходимость дальнейшего контакта** по рабочим вопросам.

### 📜 Статус и Roadmap

| **Пункт**       | **Описание**                                | **Статус**   |
| --------------- | ------------------------------------------- | ------------ |
| **База данных** | Структура категорий/товаров (20 моков).     | ✅ Готово    |
| **Код MVP**     | Главная страница и основные компоненты.     | ✅ Готово    |
| **Деплой**      | Заливка кода на хостинг.                    | ⚪️ Ожидание |
| **UI Админа**   | «Страус» (Вход по ключу, CRUD для товаров). | ⚪️ В работе |
| **Cash Out**    | Получение второй части оплаты.              | ⚪️ Ожидание |

---

### 🚀 Следующие шаги (The Ejection Plan)

1. **Deployment Target Correction:** Деплой будет осуществлен на **Firebase Hosting** (вместо Vercel) для максимальной интеграции с Firestore и упрощения инфраструктуры.
2. **Content Fill:** Залить временные стоковые фото и получить фидбэк от Клиента (для получения остатка оплаты).
3. **Final Act:** Реализовать «Протокол Страус» и передать Клиенту «ключи» от его собственного контента.

**Вывод:** Ресурсы получены. Энергия потрачена с максимальной эффективностью. Проект служит подтверждением твоего технического здоровья.

</aside>
