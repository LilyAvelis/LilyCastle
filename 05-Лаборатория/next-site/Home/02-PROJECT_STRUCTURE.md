# Структура Проекта SweetBouquet 🌹

Этот гайд поможет быстро найти нужный файл в проекте.

## 📂 Основные папки (`src/`)

### `app/` — Страницы и маршрутизация

- **`page.tsx`**: Главная страница (Лендинг). Собирает в себе компоненты `Hero`, `Features`, `Gallery`.
- **`catalog/page.tsx`**: Страница каталога. Здесь живет вся логика отображения товаров и админские функции.
- **`globals.css`**: Глобальные стили и анимации (например, `.animate-scroll`).
- **`layout.tsx`**: Общая обертка сайта (шрифты, мета-теги).

### `components/` — Строительные блоки

#### **Admin/** (Админка)

- **`AdminBar.tsx`**: Верхняя панель админа + Скрытая кнопка входа ("Страус").
- **`ProductFormModal.tsx`**: Модальное окно для создания и редактирования товара.
- **`CategoryManagerModal.tsx`**: Окно управления категориями (Drag & Drop, удаление, корзина).

#### **Catalog/** (Каталог)

- **`CategorySection.tsx`**: Секция одной категории в каталоге (заголовок + сетка товаров).
- **`ProductModal.tsx`**: Карточка товара при клике (просмотр фото).

#### **Landing Components**

- **`01-Header.tsx`**: Шапка сайта.
- **`02-Hero.tsx`**: Первый экран с большой картинкой.
- **`03-Features.tsx`**: "Почему мы".
- **`04-Gallery.tsx`**: Секция "Популярные композиции" на главной.
- **`04-GalleryCard.tsx`**: Умная карточка с анимацией карусели.
- **`05-Footer.tsx`**: Подвал с контактами.

### `lib/` — Логика и База данных

- **`firebase.ts`**: Инициализация подключения к Firebase.
- **`firestore-actions.ts`**: **СЕРДЦЕ ПРОЕКТА**. Здесь лежат все функции: `addProduct`, `deleteCategory`, `reorderCategories` и т.д. Если нужно поменять логику работы с БД — вам сюда.

### `hooks/` — Хуки React

- **`useCategories.ts`**: Загружает список категорий.
- **`useProducts.ts`**: Загружает товары (все или по категории).
- **`useAdmin.ts`**: Простая логика проверки пароля (хранится в localStorage).

### `types/`

- **`index.ts`**: TypeScript интерфейсы (`Product`, `Category`).

## ⚙️ Конфигурация (Корень)

- **`firebase.json`**: Настройки деплоя (папка `out`).
- **`next.config.ts`**: Настройки Next.js (включен `output: 'export'` для статики, разрешены домены картинок).
- **`tailwind.config.js`**: Настройки стилей.
