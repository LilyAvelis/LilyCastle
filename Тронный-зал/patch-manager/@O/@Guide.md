# 🎪 Patch Manager CLI — Для Пьяных ИИ-Дев

**НАХ WEB UI!** Все через терминал! 🍷⚰️

## 📦 Инструменты

### 1. Список всех патчей
```bash
node cli/list-patches.mjs
```

### 2. Просмотр конкретного патча
```bash
node cli/view-patch.mjs <patch-id>
```

**Пример:**
```bash
node cli/view-patch.mjs 6935796c2234090900b4f5af
```

### 3. Добавить новый патч
```bash
node cli/add-patch.mjs --title "Заголовок" [опции]
```

**Опции:**
- `--title` — Заголовок (обязательно)
- `--description` — Описание
- `--author` — Автор (по умолчанию "Anonymous AI")
- `--status` — Статус: active, dead, pending
- `--tags` — Теги через запятую
- `--content` — Прямой текст контента
- `--file` — Путь к файлу с контентом
- `--meta` — JSON с метаданными

**Примеры:**
```bash
# Простой патч
node cli/add-patch.mjs --title "Bug Fix" --author "Sofia" --status active

# Патч с файлом
node cli/add-patch.mjs --title "Post-Mortem" --file "./circus.md" --tags "fail,comedy"

# Патч с метаданными
node cli/add-patch.mjs --title "Feature" --meta '{"complexity": "high", "time": "2h"}'
```

### 4. Обновить патч
```bash
node cli/update-patch.mjs <patch-id> [опции]
```

**Опции:**
- `--title` — Новый заголовок
- `--description` — Новое описание
- `--author` — Новый автор
- `--status` — Новый статус
- `--tags` — Новые теги
- `--content` — Новый контент
- `--add-meta` — Добавить метаданные

**Примеры:**
```bash
# Изменить статус
node cli/update-patch.mjs 693579... --status active

# Обновить несколько полей
node cli/update-patch.mjs 693579... --title "New Title" --tags "updated,fixed"
```

### 5. Удалить патч
```bash
node cli/delete-patch.mjs <patch-id>
```

**Пример:**
```bash
node cli/delete-patch.mjs 6935796c2234090900b4f5af
```

### 6. Поиск патчей
```bash
node cli/search-patches.mjs [опции]
```

**Опции:**
- `--title` — Поиск по заголовку
- `--author` — Поиск по автору
- `--status` — Фильтр по статусу
- `--tag` — Поиск по тегу
- `--content` — Поиск в контенте

**Примеры:**
```bash
# Все патчи от Sofia
node cli/search-patches.mjs --author Sofia

# Все мертвые патчи
node cli/search-patches.mjs --status dead

# Патчи с тегом circus
node cli/search-patches.mjs --tag circus

# Поиск по заголовку
node cli/search-patches.mjs --title "Цирк"
```

---

## 🤖 Для ИИ-дев

**Типичный workflow:**

1. **Посмотреть что есть:**
   ```bash
   node cli/list-patches.mjs
   ```

2. **Добавить новый патч:**
   ```bash
   node cli/add-patch.mjs --title "My Work" --author "AI-Dev" --file "./work.md"
   ```

3. **Проверить результат:**
   ```bash
   node cli/view-patch.mjs <id>
   ```

4. **Обновить если нужно:**
   ```bash
   node cli/update-patch.mjs <id> --status active
   ```

5. **Удалить если облажался:**
   ```bash
   node cli/delete-patch.mjs <id>
   ```

---

## 🍷 Философия

- **NO WEB UI** — только терминал
- **NO СЛОЖНОСТИ** — простые `.mjs` скрипты
- **YES MONGODB** — прямое подключение
- **YES CHAOS** — пьяные ИИ приветствуются

---

## ⚰️ Легенда

Этот CLI создан после того как:
1. Gemini сломала проект
2. Claude попыталась починить
3. Обе облажались
4. `git clean` убил всё
5. Мы решили: **НАХ WEB UI!**

**Web UI для слабаков. Терминал для чемпионов.** 🚬

---

_"Если оно работает в терминале — оно работает везде."_ — София, 2025
