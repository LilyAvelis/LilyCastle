---
applyTo: "**"
---

## 👁️ Garden Eye — кратко для агента

### ① Зачем

Не читать 500+ строк простынёй, а сначала увидеть **структуру**, потом уже лезть в код.

---

### ② Два инструмента

1. `skeleton` — рентген файла
   - Показывает **структуру без кода**: классы, функции, методы, диапазоны строк.
   - Вызов (через MCP):
     ```
     mcp_garden-eye_skeleton(filePath: "путь/к/файлу.ts")
     ```
   - Использование:
     - понять, какие сущности есть в файле;
     - решить, с какой работать.
2. `focus` — лупа
   - Показывает **только один элемент** по имени.
   - Вызов:
     ```
     mcp_garden-eye_focus(
       filePath: "путь/к/файлу.ts",
       target: "имя_функции_или_класса"
     )
     ```
   - Использование:
     - прочитать ровно тот метод/функцию, который нужен;
     - менять код, не теряя контекст.

---

### ③ Правильный workflow

```
1) skeleton("bigFile.ts")
   → смотрю список классов/функций

2) выбираю нужный элемент (например, "handleMessage")

3) focus("bigFile.ts", "handleMessage")
   → читаю только его

4) после этого предлагаю/делаю правку
```

---

### ④ Что поддерживается

- `.ts`, `.tsx`, `.js`, `.jsx` → классы, методы, функции, интерфейсы.
- `.md` → заголовки `# ## ###` как оглавление.

---

### ⑤ Минимальные тесты

- `skeleton(".../chatViewProvider.ts")`
- `skeleton(".../Home/[Base.md](http://Base.md)")`
- `focus(".../chatViewProvider.ts", "getAgentName")`

---

Мета-принцип: **«Сначала skeleton, потом focus, потом правка. Никогда наоборот.»**
