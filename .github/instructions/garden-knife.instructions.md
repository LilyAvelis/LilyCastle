---
applyTo: "**"
---

Вот сжатый гайд для ИИ по Garden Knife:

---

## 🔪 Garden Knife — кратко для агента

### ① Зачем

Не искать `oldString` и не переписывать полфайла.

Knife работает по **адресу элемента**, а не по кускам текста.

---

### ② Два инструмента

1. `knife_preview` — посмотреть, что будет вырезано

   ```
   mcp_garden-knife_knife_preview(
     filePath: "путь/к/файлу.ts",
     target:  "ClassName.methodName"
   )
   ```

   Использование:

   - проверяешь, тот ли метод/класс/секция выбрана;
   - видишь диапазон строк до замены.

2. `knife` — заменить элемент

   ```
   mcp_garden-knife_knife(
     filePath: "путь/к/файлу.ts",
     target:  "ClassName.methodName",
     newCode: "полный_новый_код_элемента"
   )
   ```

   Использование:

   - передаёшь **полный текст метода/функции/класса**;
   - Knife сам ставит отступы и аккуратно заменяет только этот элемент.

---

### ③ Как адресовать код

- `ClassName.methodName` → метод класса
- `ClassName` → весь класс
- `functionName` → top‑level функция
- `## Заголовок` → секция в Markdown

Примеры:

- `Player.takeDamage` → метод `takeDamage` в классе `Player`
- `Player` → весь класс `Player`
- `greet` → функция `greet`
- `## Установка` → секция «Установка» в `.md`

---

### ④ Правильный workflow (с Eye + Knife)

```
1) skeleton("file.ts") → вижу структуру
2) focus("Player.takeDamage") → читаю только метод
3) knife(..., target: "Player.takeDamage", newCode: "...") → точечная замена
```

---

### ⑤ Правила

1. `newCode` всегда с **полной сигнатурой** (целиком метод/функция/класс).
2. Отступы и форматирование Knife подстраивает сам.
3. Нормальный порядок: `skeleton` → `focus` → `knife`.
