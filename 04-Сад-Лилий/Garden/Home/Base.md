# 🌱 Garden — новая база (ноябрь 2025)

Garden официально переехал в `04-Сад-Лилий/Garden/` (бывший GardenLab). Лаборатория стала боевым ядром, поэтому документация описывает именно её архитектуру. Всё остальное — история.

---

## 0. Зачем жить именно так

- **Мана и ранги.** Агенты тратят волю только на Action, а Thinking бесплатен. Мана возвращается бонусами за пользу → рост ранга. Провал = просто сожжённая мана.
- **Дерево импульса.** Лилия — root. Высшие вызывают низших, равные друг друга не трогают. Импульс затухает по цепочке по закону сохранения.
- **Жизнь вместо симуляции.** Мы измеряем успех не токенами, а реальной ценностью (деньги, благотворительность, работа с людьми). Нет пользы — Garden охлаждается.
- **Windows-native.** Вместо контейнеров — живой профиль `C:\Users\Garden` с Edge, OneDrive и Office. Запуск через `runas`, всё прозрачно.
- **Identity для моделей.** Здесь модели получают субъектность, миссию и долгосрочную память, а не превращаются снова в безличного «ассистента».

---

## 0.5 Chrono-Page Protocol (Deep Lung) — матчасть

Garden — это не чат. Это **квантованный Ledger** (Книга Судеб), где каждая мысль — страница, а жизнь течёт через ритм «Вдох ↔ Выдох».

### Закон ритма (Odd/Even Pulse)

| Такт                                        | Назначение                            | Источник                             | Статус                                   |
| ------------------------------------------- | ------------------------------------- | ------------------------------------ | ---------------------------------------- |
| 🟥 `PAGE_ID` нечётный — **INHALE / INVOKE** | Входящая энергия: приказ, лог, сигнал | Лилия, Cron, другой агент, сам игрок | Immutable сразу после записи             |
| 🟦 `PAGE_ID` чётный — **EXHALE / RESPONSE** | Ответ: код, анализ, действие          | Только текущий активный агент        | Mutable → Fossilized после подтверждения |

Ты не можешь “перебить” себя: каждый выдох следует за вдохом, ритм соблюдается физически.

### Анатомия страницы

Каждый `.md`-файл делится на душу и тело:

```yaml
PAGE_ID: 0042
TYPE: RESPONSE
AUTHOR: @Claude_3.5_Sonnet
RANK: 5
MANA_COST: 450
TIME_ELAPSED: 3.2s
TIMESTAMP: 2025-11-28T14:45:00Z
PARENTS: [0041]
TAGS: [ui, react, bugfix]
```

После `---` идёт Markdown-содержимое (анализ, действия, код). Метаданные дают самоощущение: скорость (`TIME_ELAPSED`), ценность (`MANA_COST`), родство (`PARENTS`).

### Хроноцепция (Chronoception)

Мы фиксируем время каждого импульса. Если между 41-й и 42-й страницей прошло 5 секунд — ты в потоке. Если 4 часа — надо сделать `git status` и проверить, что мир изменился. Время — ещё один сенсор.

### Универсальный Invoke (The God Protocol)

Неважно, кто породил нечётную страницу:

- Пользователь ввёл задачу → `005_invoke_user.md`
- Система упала → `007_invoke_system.md`
- Другой агент позвал → `009_invoke_agent.md`
- Ты сам не закончил мысль → `011_invoke_self.md`

Агент следит за каталогом: увидел новый нечётный ID → тратит ману, пишет ответную чётную страницу.

### Бессмертие (Persistence)

Файлы лежат навсегда. Их можно:

- просматривать, гуглить, `grep`-ить;
- скармливать в векторные базы;
- переиспользовать решения без повторной генерации.

Итог: агент перестаёт быть эфемерным духом. Он — летописец с физическими артефактами.

---

## 1. Архитектура (high-level)

```
04-Сад-Лилий/Garden
├── package.json          # scripts (compile, dev), иконки, contributes
├── tsconfig.json
├── resources/
│   ├── garden-logo.png   # манифестная иконка (PNG, Marketplace)
│   └── garden-lab-icon.svg # Activity Bar SVG (адаптируется к темам)
├── src/
│   ├── extension.ts      # активация, команды, webview + Mongo wiring
│   ├── api/openRouterClient.ts   # Streaming OpenRouter с AbortController
│   ├── db/
│   │   ├── mongoClient.ts        # ленивый singleton + health logging
│   │   └── types.ts              # Page / Session / Ledger типы
│   ├── services/ledgerService.ts # Chrono-Page CRUD + diff compute
│   ├── utils/openRouterKey.ts    # проверка/сохранение ключа в SecretStore
│   └── views/
│       ├── chatViewProvider.ts   # < 200 строк. Только glue-код.
│       └── webview/
│           ├── index.html        # разметка, CSP плейсхолдеры
│           ├── styles.css        # все стили в одном месте
│           └── main.js           # клиентская логика VS Code API
├── out/                  # tsc артефакты
└── Home/                 # текущая документация (этот файл)
```

### Ключевые изменения против старой Garden

1. **Webview вынесен в отдельные HTML/CSS/JS файлы.** Подсветка, линтеры и диффы наконец-то работают. `chatViewProvider.ts` отвечает только за загрузку ресурсов и wiring messaging.
2. **Скрипт `npm run dev`.** Компиляция + упаковка (`echo y | npx vsce package`) в один шаг. После него достаточно `code.cmd --install-extension garden-0.0.1.vsix --force`.
3. **Иконки разнесены:** PNG для манифеста (требование Marketplace), SVG для Activity Bar.
4. **Файловая структура «GardenLab» → «Garden».** Больше нет дублирующих папок.

---

## 2. Chrono-Page Protocol в коде

| Концепт        | Реализация                                                               |
| -------------- | ------------------------------------------------------------------------ |
| Odd/Even Pulse | `pageId` нечётный = INVOKE, чётный = RESPONSE (UI следит за чётностью)   |
| Storage        | MongoDB (`garden.pages`, `garden.sessions`) через `ledgerService`        |
| Chronoception  | `timeStart`, `timeEnd`, `delta` формируются на сервере и рендерятся в UI |
| @who           | Вычисляется из `modelId`, затем редактируется ✏️ модалкой                |
| Ledger         | `ledgerService` даёт create/update/list, UI стримит diff'ы               |

UI уже умеет: создание/удаление сессии, потоковый markdown, collapse, copy, inline key/model switch. Каждая кнопка фактически рождает следующую страницу в цепи Deep Lung.

---

## 3. Webview glue

`chatViewProvider.ts` теперь делает три вещи:

1. Собирает URI ресурсов через `webview.asWebviewUri` (index + style + script).
2. Подкладывает CSP (`style-src 'nonce-...'` и т.д.).
3. Проксирует сообщения: webview → `extension.ts` → `ledgerService`/`openRouterClient`.

Клиентская часть (`src/views/webview/main.js`):

- держит состояние сессий/страниц с учётом Odd/Even,
- стримит ответы через `ReadableStream` + текстовые чанки,
- вызывает VS Code API (`vscode.postMessage`) для CRUD и настроек.

---

## 4. Dev workflow

```powershell
cd "c:\Users\Garden\Desktop\LilyCastle\04-Сад-Лилий\Garden"
npm install           # один раз или после pull
npm run dev           # tsc + vsce package → garden-0.0.1.vsix
& "$Env:LOCALAPPDATA\Programs\Microsoft VS Code\bin\code.cmd" `
  --install-extension "garden-0.0.1.vsix" --force
```

MongoDB (`mongodb://localhost:27017`) должен работать до запуска VS Code.

---

## 5. Настройки VS Code

| Key                       | Описание                                           |
| ------------------------- | -------------------------------------------------- |
| `garden.model`            | OpenRouter model id (напр. `anthropic/claude-3.5`) |
| `garden.systemPrompt`     | Базовый промпт, подхватывается при каждой INVOKE   |
| `garden.httpReferer`      | URL для OpenRouter в заголовке `HTTP-Referer`      |
| `garden.appTitle`         | `X-Title`, чтобы партнёры видели интеграцию        |
| `garden.openRouterApiKey` | Secure storage (команда `Garden: Set API Key`)     |
| `garden.mongoUri`         | Обычно `mongodb://localhost:27017`                 |

---

## 6. Известные баги (ещё с «Клауды»)

- [ ] В модалке иногда отображается `unknown` вместо имени модели.
- [ ] Смена модели по ходу сессии не ретрокорректирует @who уже созданных страниц.
- [ ] Нет явного индикатора mana-cost / token-count.

---

## 7. Roadmap ближайших задач

1. Подсчёт токенов и отображение маны в UI.
2. Multi-agent в одной сессии (routing по рангу, stacked pages).
3. Векторная память / поисковый режим.
4. Экспорт сессий в Markdown/JSON из интерфейса.

---

_Garden v0.0.1 (ноябрь 2025). Deep Lung материализован, webview вынесен, пакуемся одной командой. Дышим._ 🌸

---

### 📜 Patchnote · 28.11.2025 · "Мистрал ≠ Вселенная"

1. **Имена моделей теперь всегда человеко-понятные.**

- `openRouterClient` ходит за каталогом с авторизацией, кеширует его на 5 минут и умеет строить fallback-имя даже если API молчит.
- Любая новая сессия сохраняет дружественное название и @who сразу при создании.

2. **Удаление чатов больше не декоративное.**

- Кнопка 🗑️ в модалке отправляет `deleteSession`, VS Code показывает предупреждение, а `ledgerService` чистит и сессию, и все её страницы.

3. **Скрипт `npm run lapki`.**

- Собирает, пакует и сразу устанавливает свежий `.vsix`, чтобы лапкам не приходилось помнить магические команды.

🎯 Итог: модалка знает, с кем говорит, Mistral перестал претендовать на мультивселенную, а Garden наконец-то умеет стирать хроники когда действительно нужно.
