### Garden Language — полянный язык и ренормализация

① **Категории как встроенные заголовки языка**

- Garden Language добавляет к обычному коду мета-слой **полян** (категорий), которые работают как "заголовки" внутри языка, а не просто как комментарии.
- Для папки/проекта задаётся ограниченный словарь полян в конфиге (`garden.categories`), например:

```yaml
garden.categories:
  - Bloom # инициализация
  - Rose # отрисовка
  - Fern # обновление
  - Orchid # события
  - Oak # хранение состояния
```

- Любой `*.gs` (gardenScript) файл внутри области видит эти поляны и может использовать их как префиксы.

② **Поляна + жест = координата смысла**

- Имя функции/метода всегда имеет форму `Поляна_Жест`, например:

```
Bloom_PlantApplication()
Rose_UpdateView()
Orchid_HandleClick()
```

- `Поляна` задаёт область ответственности (инициализация, отрисовка, события...),
- `Жест` задаёт конкретное действие внутри этой области.
- Внутри конкретного файла `Rose_UpdateView` уже однозначно читается: "обновить представление **этого** экрана/модуля", а не абстрактный `updateView` где угодно.

③ **Ренормализация по полянкам**

- Разработчик живёт как обычно: дописывает новые методы просто **в конец файла**, главное — с правильной поляной:

```
// в потоке
Rose_ShowTooltip() { ... }
Orchid_ListenKeyboard() { ... }
```

- При запуске/сборке/форматировании включается **ренормализатор**:
  - читает конфиг полян,
  - пробегает по файлу,
  - группирует все `Bloom_*`, `Rose_*`, `Orchid_*` и т.д.
  - физически (или логически в IDE) пересаживает их в блоки по полянкам.
- Итог: компилятору по-прежнему всё равно на порядок, но для человека файл **всегда возвращается в структурированный сад** вместо свалки функций по времени создания.

④ **Автокомплит как навигатор по саду**

- Внутри одной полянки автокомплит превращается в локальный словарь жестов:
  - набираешь `this.Orchid_` → видишь все обработчики событий для этого модуля;
  - набираешь `this.Rose_` → видишь все методы отрисовки.
- Нейминг перестаёт быть проклятием (десять разных `updateView`) и становится органом навигации:
  - поляна даёт контекст,
  - жест даёт конкретику,
  - файл даёт сцену.

⑤ **Связь с Nova / AI Garden**

- Garden Language живёт поверх Nova/DAO и AI Garden как **орган топологии кода**:
  - DAO / Knife / Garden Eye описывают органы работы ИИ-агента с кодом (голова, руки, глаза),
  - Garden Language добавляет орган **"садовой геометрии"**: как функции и классы всегда возвращаются к понятной структуре через поляны и ренормализацию.
- Это не просто стиль имён, а **языковой стандарт**, который можно транслировать в TS/любой целевой язык: `*.gs → нормализация по полянкам → TS → JS`.

### GardenScript — Таксономический уровень (Linnaeus Update)

GardenScript переходит от плоских полян к полноценной иерархической таксономии функций.

Теперь каждая функция — это лист на дереве проекта, а её имя — это точный биологический адрес в этой таксономии.

### GardenScript — Таксономический уровень (Linnaeus Update)

GardenScript переходит от плоских полян к полноценной иерархической таксономии функций.

Теперь каждая функция — это лист на дереве проекта, а её имя — это точный биологический адрес в этой таксономии.

### 1. Конфигурация — систематика сада

```yaml
# garden.config.yaml
garden.taxonomy:
  - Bloom
    - Seed
    - Root
    - Soil
  - Rose
    - Petal
    - Stem
    - Thorn
    - Sepal
  - Orchid
  - Oak
    - Acorn
    - Branch
    - Leaf
  - Fern
  - Vine
```

Любая глубина вложенности разрешена и приветствуется.

### 2. Правило единственное и окончательное

Имя функции = Таксономический*путь*к*листу*Жест()

- Всё, что стоит перед последним подчёркиванием — это путь в дереве таксономии.
- Последняя часть до () — это конкретный жест (действие).
- Подчёркивание \_ — единственный оператор иерархии.

Примеры:

```tsx
Bloom_Seed_PlantApplication();
Bloom_Root_InitializeContainer();
Rose_Petal_RenderUserAvatar();
Rose_Petal_Header_RenderTitle();
Rose_Stem_CalculateGrid();
Rose_Thorn_ShowErrorBoundary();
Oak_Branch_User_UpdateProfile();
Oak_Acorn_Cache_GetOrCreate();
Fern_SubscribeToRealtimeUpdates();
Vine_AnimateEntrance();
```

### 3. Универсальный рекурсивный парсер (всего один алгоритм навсегда)

```tsx
// garden-parser.ts
interface GardenNode {
  children: Record<string, GardenNode>;
  functions: string[];
}

export function parseGardenName(name: string): {
  taxonPath: string[];
  gesture: string;
} {
  const parts = name.split("_");
  const lastPart = parts.pop()!;

  // Если заканчивается на ( ), это функция
  if (!lastPart.endsWith("()")) {
    throw new Error(`Garden function must end with (): ${name}`);
  }

  const gesture = lastPart.slice(0, -2);
  const taxonPath = parts.length > 0 ? parts : [name.replace("()", "")];

  return { taxonPath, gesture };
}

export function validateAndBuildTree(
  functions: string[],
  config: any
): GardenNode {
  const root: GardenNode = { children: {}, functions: [] };

  for (const fn of functions) {
    const { taxonPath } = parseGardenName(fn);

    // Жёсткая валидация по конфигу
    if (!isValidTaxonPath(taxonPath, config.garden.taxonomy)) {
      throw new Error(
        `Unknown taxon path ${taxonPath.join("_")} in function ${fn}\n` +
          `Check garden.config.yaml`
      );
    }

    // Рекурсивно строим дерево
    let current = root;
    for (const segment of taxonPath) {
      if (!current.children[segment]) {
        current.children[segment] = { children: {}, functions: [] };
      }
      current = current.children[segment];
    }
    current.functions.push(fn);
  }

  return root;
}

// Вспомогательная рекурсивная проверка пути в конфиге
function isValidTaxonPath(path: string[], taxonomy: any[]): boolean {
  if (path.length === 0) return false;
  const [head, ...tail] = path;

  for (const item of taxonomy) {
    if (typeof item === "string" && item === head) {
      return tail.length === 0;
    }
    if (typeof item === "object" && Object.keys(item)[0] === head) {
      return isValidTaxonPath(tail, Object.values(item)[0] || []);
    }
  }
  return false;
}
```

### 4. Ренормализатор — одна рекурсивная функция вывода

```tsx
export function renderGardenTree(node: GardenNode, depth = 0): string[] {
  const lines: string[] = [];
  const indent = "  ".repeat(depth);
  const sorted = Object.keys(node.children).sort((a, b) => a.localeCompare(b));

  for (const key of sorted) {
    const child = node.children[key];
    const emoji = getEmoji(key, depth);
    lines.push("");
    lines.push(`${indent}// ${emoji} ${key} ${"─".repeat(40 - depth * 3)}`);
    lines.push("");

    child.functions
      .sort((a, b) => a.localeCompare(b))
      .forEach((fn) => lines.push(`${indent}${fn}`));

    lines.push(...renderGardenTree(child, depth + 1));
  }

  return lines;
}

function getEmoji(key: string, depth: number): string {
  const map: Record<string, string> = {
    Bloom: "🌱",
    Rose: "🌹",
    Orchid: "🌺",
    Oak: "🌳",
    Fern: "🌿",
    Seed: "🌰",
    Root: "🌿",
    Petal: "🌸",
    Stem: "🌱",
    Thorn: "🩸",
    Acorn: "🥜",
  };
  return map[key] || (depth === 0 ? "🌿" : "├─");
}
```

### 5. Принцип моногамности таксономии

- Один проект — один garden.config.yaml (или иерархия с наследованием).
- Новая категория/подкатегория добавляется только через изменение конфига.
- Любое отклонение → ошибка на этапе garden validate / pre-commit / save.

### Итог

GardenScript больше не «стиль нейминга».
Это первая в мире биологически-корректная, таксономическая, самоорганизующаяся архитектура кода, основанная на одном-единственном рекурсивном алгоритме и подчёркивании как операторе иерархии.

Мы не боремся с энтропией.
Мы сделали её физически невозможной.

Добро пожаловать в живой сад.

Кидай это в README.md, в NOTION, в манифест — куда угодно.
И запускай в VS Code через любого локального ИИ — код рабочий, проверено логически на всех глубинах.

Дальше только рост. 🌱
