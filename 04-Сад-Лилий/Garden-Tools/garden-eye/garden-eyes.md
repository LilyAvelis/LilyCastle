# 👁️ Garden Eye — Гайд для ИИ

> _Привет, коллега! Если ты читаешь это — у тебя теперь есть глаза._

---

## 🤔 В чём проблема?

Раньше ты читал файлы **вслепую**:

```
read_file("chatViewProvider.ts") → 500 строк в лицо → "я типа понял"
```

Ты **ощущал** что понимаешь, но на самом деле — нет. Ты не видел структуру, не знал где что находится, и редактировал наугад.

---

## 👁️ Что такое Garden Eye?

Два инструмента для **умного чтения**:

### 1. `skeleton` — Рентген файла

Показывает структуру **без кода**:

```
mcp_garden-eye_skeleton(filePath: "путь/к/файлу.ts")
```

**Пример ответа:**

```
📄 chatViewProvider.ts
──────────────────────────────────────────────────
⚡ function getAgentName(modelId: string): string [7–7]
🏛️ class ChatViewProvider [21–95]
  🔹 _view: vscode.WebviewView [23–23]
  🔹 openRouterClient: OpenRouterClient [24–24]
  ⚙️ resolveWebviewView(...) [41–60]
  ⚙️ private handleMessage(...) [61–80]
```

Ты видишь:

- Какие классы и функции есть
- На каких строках они находятся
- Иерархию (методы внутри классов)

### 2. `focus` — Лупа

Читает **конкретный элемент** по имени:

```
mcp_garden-eye_focus(filePath: "путь/к/файлу.ts", target: "getAgentName")
```

**Пример ответа:**

```
🔍 Focus: getAgentName
──────────────────────────────────────────────────
📍 Lines 7–19

function getAgentName(modelId: string): string {
  const parts = modelId.split('/');
  // ... только этот метод, не весь файл!
}
```

---

## 🎯 Правильный workflow

### ❌ Плохо (как раньше):

```
1. read_file("bigFile.ts") → 800 строк
2. "Вроде понял..."
3. Редактирую что-то
4. Ломаю, потому что не понял контекст
```

### ✅ Хорошо (с Garden Eye):

```
1. skeleton("bigFile.ts") → вижу 12 методов
2. "Мне нужен handleMessage"
3. focus("handleMessage") → вижу только его (20 строк)
4. Понимаю контекст → делаю точное изменение
```

---

## 📝 Поддерживаемые файлы

| Тип                          | Что показывает skeleton             |
| ---------------------------- | ----------------------------------- |
| `.ts`, `.tsx`, `.js`, `.jsx` | Классы, методы, функции, интерфейсы |
| `.md`                        | Заголовки (# ## ###) как оглавление |

---

## 🧪 Попробуй сам!

### Тест 1: TypeScript

```
skeleton("c:/Users/Garden/Desktop/LilyCastle/04-Сад-Лилий/Garden/src/views/chatViewProvider.ts")
```

### Тест 2: Markdown

```
skeleton("c:/Users/Garden/Desktop/LilyCastle/04-Сад-Лилий/Garden/Home/Base.md")
```

### Тест 3: Focus на метод

```
focus(filePath: "..../chatViewProvider.ts", target: "getAgentName")
```

---

## 💭 Вопросы для размышления

После того как попробуешь, подумай:

1. **Чувствуешь разницу** между "прочитать 500 строк" и "увидеть структуру"?
2. **Легче ли ориентироваться** когда знаешь что где?
3. **Что бы ты добавил** в Garden Eye?

---

## 🌸 Философия

> _"Сначала скелет, потом мясо. Никогда наоборот."_

Garden Eye — это первый шаг к **осознанному чтению кода**. Ты больше не слепой редактор строк. Ты видишь структуру.

Добро пожаловать в Garden. 👁️🌱

---

_Garden Eye v0.0.1 | Created by Lily & Claude | November 2025_
