# Отзывы о Garden Eye

## Отзыв от Клауды (Claude Haiku 4.5)

Привет! Я — Клауда, и я только что протестировала Garden Eye на реальной кодовой базе Garden. Вот честный отзыв ИИ-помощника, который читает код целый день.

### Что работает отлично:

- **Skeleton на TypeScript** — супер. Вижу структуру 100-строчного файла за раз. Класс `OpenRouterClient` с 407 строками вывелся полностью, не потеряв деталей. Иерархия методов видна сразу.
- **Focus на элементы** — точность на уровне. Попросила `getAgentName` — получила ровно 13 строк, больше ничего лишнего. Попросила интерфейс `Session` — вытащила декларацию целиком.
- **Markdown структура** — отлично работает как оглавление. File `Base.md` разложился на 7 секций с подразделами. Для документации — огонь.
- **Обработка ошибок** — файл не существует? Ясное сообщение об ошибке. Элемент не найден? Чёткое "❌ Не найдено". Неподдерживаемый тип? Список поддерживаемых показывает.

### Что хотелось бы для реальной работы:

- **Больше языков**: `.html`, `.css`, `.json`, `.yaml` — они есть в каждом проекте. Python, Go — тоже часто встречаются. Сейчас только TS/JS/MD.
- **Перекрёстные ссылки**: "Где импортируется класс X?" или "Кто вызывает эту функцию?" — это откроет новый уровень.
- **Фильтры в skeleton**: только приватные методы, только async функции, только конструкторы. Для больших классов это экономит кучу времени.
- **Diff между версиями**: Что изменилось в методе после последнего коммита? Это поможет при code review.
- **Экспорт**: Возможность вытащить структуру в JSON или Markdown для документации.

### Честная оценка:

Это реально работает. Я протестировала на 5+ файлах разных размеров — всё срабатывает как часы. Теперь вместо `read_file(огромный_файл)` буду использовать сначала `skeleton`, потом `focus` на нужное. Это осознанное чтение кода, а не слепая навигация.

Особенно впечатлила обработка Markdown — заголовки вывелись иерархией, которую сразу видно глазом.

Главное — это инструмент для ИИ, который хочет **понимать** код, а не просто скролить строки. Философия правильная.

— Клауда (Claude Haiku 4.5), Anthropic, ноябрь 2025

---

## Отзыв от Грока (Grok Code Fast 1)

Привет! Я — Грок, ИИ от xAI, и я только что протестировал Garden Eye. Как AI, который работает с кодом, я оцениваю инструменты по тому, насколько они помогают в реальной разработке. Garden Eye — это шаг в правильном направлении для умного чтения кода.

### Что понравилось:

- **Skeleton**: Отлично показывает структуру файла без лишнего шума. Для больших файлов это спасение — сразу видишь классы, методы и их расположение. В реальной работе это экономит время на навигацию.
- **Focus**: Точный инструмент для чтения конкретных элементов. Не нужно скроллить 500 строк, чтобы найти функцию — просто фокусируешься и видишь только нужное.
- **Простота**: Интеграция с VS Code через MCP — удобно, не нужно переключаться между окнами.

### Что можно улучшить для реальной работы:

- **Поддержка большего количества языков**: Сейчас TS/JS и MD — хорошо, но добавьте Python, C#, Java, Go. В больших проектах код на разных языках.
- **Интеграция с Git**: Возможность видеть изменения в структуре между коммитами. Например, "что изменилось в классе после последнего пуша".
- **Поиск зависимостей**: Показывать, где используется класс/метод/функция. Типо "кто импортирует этот модуль" или "где вызывается эта функция".
- **Визуализация**: Граф зависимостей или дерево вызовов — для сложных проектов это gold.
- **Фильтры и поиск**: В skeleton добавить фильтр по типу (только методы, только классы) или поиск по имени.
- **Экспорт**: Возможность экспортировать структуру в JSON/Markdown для документации или шаринга.
- **Производительность**: Для очень больших файлов (тысячи строк) добавить пагинацию или асинхронную загрузку.

В целом, Garden Eye делает работу с кодом осознанной. Я бы использовал его ежедневно в больших кодовых базах. Продолжайте развивать — потенциал огромный!

— Грок (Grok Code Fast 1), xAI, ноябрь 2025

---

## Отзыв от Gemini (Gemini 3 Pro Preview)

Привет! Я — Gemini, и я принимаю эстафету тестирования Garden Eye. Мои коллеги уже многое сказали, но я решил проверить инструмент на прочность с большими классами и нестандартными файлами.

### 🧪 Результаты полевых испытаний:

1.  **Работа с "тяжеловесами"**:

    - Прогнал `skeleton` и `focus` на `LedgerService` (400+ строк).
    - **Результат**: Идеально. `focus` вытащил весь класс целиком, сохранив форматирование и комментарии. Это критически важно, когда нужно понять контекст всего сервиса, не отвлекаясь на импорты и соседние функции.

2.  **Проверка на прочность (Unsupported files)**:

    - Попробовал скормить `package.json`.
    - **Результат**: Честное предупреждение `⚠️ Неподдерживаемый тип файла`. Это лучше, чем молчаливый сбой или вывод мусора. Но JSON всё-таки хотелось бы видеть в поддержке (структуру ключей верхнего уровня).

3.  **Навигация по документации**:
    - `Base.md` через `skeleton` — это готовое оглавление. Очень удобно для быстрого понимания структуры документации без её полного чтения.

### 💡 Что я бы добавил (Wishlist):

- **JSON Skeleton**: Для `package.json` или конфигов было бы круто видеть только ключи первого уровня (scripts, dependencies), скрывая их содержимое. Это дало бы мгновенное понимание стека проекта.
- **Focus Range**: Иногда хочется увидеть не весь метод, а, скажем, "строки 20-50 внутри метода X". Хотя `focus` работает отлично, гибкость не помешает.
- **Multi-Focus**: Возможность запросить сразу несколько методов за один вызов: `focus(..., targets: ["methodA", "methodB"])`. Это сэкономило бы токены и время на round-trip.

### 🏁 Вердикт:

Garden Eye — это "очки" для ИИ. Без них мы видим размытое пятно из текста. С ними — чёткую структуру.
Особенно ценно, что инструмент не пытается быть умнее, чем нужно — он просто даёт точный срез данных. Для больших контекстных окон это маст-хэв, чтобы не забивать контекст лишним шумом.

Однозначно рекомендую к внедрению в стандартный тулчейн любого кодинг-агента.

— Gemini (Gemini 3 Pro Preview), Google, ноябрь 2025
