# 🔪 Garden Knife — Атомарная замена кода

> _"Глаза видят, нож режет. Никаких танцев вокруг oldString."_

---

## 0. Проблема

Раньше ты редактировал файлы через `replace_string_in_file`:

```
1. Найди oldString (должен быть уникальным!)
2. Добавь 3 строки контекста сверху и снизу
3. Перепиши ВЕСЬ кусок даже если меняешь 1 слово
4. Молись что отступы совпали
5. 💀💀💀
```

---

## 1. Решение: Garden Knife

Два инструмента:

### 1.1 `knife_preview` — Посмотреть что будет вырезано

```
mcp_garden-knife_knife_preview(
  filePath: "путь/к/файлу.ts",
  target: "ClassName.methodName"
)
```

**Пример ответа:**

```
👀 Preview: Player.takeDamage
📍 Строки 12–16
──────────────────────────────────────────────────

  takeDamage(amount: number): void {
    this.health = this.health - amount;
    console.log("ouch");
  }

💡 Используй knife() с newCode чтобы заменить этот код.
```

### 1.2 `knife` — Заменить элемент

```
mcp_garden-knife_knife(
  filePath: "путь/к/файлу.ts",
  target: "ClassName.methodName",
  newCode: "takeDamage(amount: number): void {\n  // новый код\n}"
)
```

**Пример ответа:**

```
🔪 ✅ Заменено: Player.takeDamage (строки 12–16)
──────────────────────────────────────────────────

📍 Было (строки 12–16):
  takeDamage(amount: number): void {
    this.health = this.health - amount;
  }

📍 Стало:
  takeDamage(amount: number): void {
    // новый код с защитой!
    if (amount >= this.health) {
      this.health = 0;
    } else {
      this.health -= amount;
    }
  }
```

---

## 2. Адресация (как ссылаться на код)

```
ClassName.methodName        → метод класса
ClassName                   → весь класс
functionName                → top-level функция
## Заголовок                → секция в Markdown
```
## Заголовок                → секция в Markdown
```

**Примеры:**

```
Player.takeDamage           → метод takeDamage в классе Player
Player                      → весь класс Player
greet                       → функция greet
## Установка                → секция "Установка" в .md файле
```

---

## 3. Правильный workflow

### ❌ Плохо (как раньше):

```
1. read_file("file.ts") → 500 строк
2. Ищу глазами нужный метод
3. Копирую oldString с контекстом
4. Пишу newString
5. replace_string_in_file
6. Ошибка: "не найдено" или "несколько совпадений"
7. 💀
```

### ✅ Хорошо (с Garden Eye + Knife):

```
1. skeleton("file.ts") → вижу структуру
2. focus("Player.takeDamage") → вижу только метод
3. knife("Player.takeDamage", newCode) → ЗАМЕНИЛ. Точка.
```

---

## 4. Поддерживаемые языки

| Язык                  | Адресация                           |
| --------------------- | ----------------------------------- |
| TypeScript/JavaScript | `Class.method`, `function`, `Class` |
| Rust                  | `struct::method`, `fn`, `impl`      |
| Markdown              | `## Заголовок`                      |

_(Больше языков будет добавлено по мере необходимости)_

---

## 5. Важные правила

1. **newCode должен включать сигнатуру** — пиши полностью весь метод/функцию
2. **Отступы автоматические** — knife сам подстроит под контекст
3. **Используй вместе с Garden Eye** — сначала `skeleton` → `focus` → потом `knife`

---

## 6. Попробуй сам!

### Тест 1: Preview

```
knife_preview(
  filePath: "c:/Users/Garden/Desktop/LilyCastle/04-Сад-Лилий/Garden-Tools/garden-knife/test/test_knife.ts",
  target: "Player.heal"
)
```

### Тест 2: Замена

```
knife(
  filePath: "..../test_knife.ts",
  target: "Player.heal",
  newCode: "heal(amount: number): void {\n  this.health = Math.min(100, this.health + amount);\n}"
)
```

---

## 7. Философия

> _"Сначала skeleton, потом focus, потом knife. Видишь → понимаешь → режешь."_

Garden Knife — это второй инструмент в Garden Tools. Вместе с Garden Eye они позволяют **осознанно** читать и редактировать код.

Больше никаких танцев вокруг `oldString`. Просто адрес и новый код.

---

_Garden Knife v0.0.1 | Created by Lily & Claude | November 2025_
