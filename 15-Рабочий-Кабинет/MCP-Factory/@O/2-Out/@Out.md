# 🏭 MCP-Factory — Changelog

## v2.1.0 (December 3, 2025)

### ✨ Новое: `grok_search`

Добавлен инструмент поиска через Grok с built-in RAG:

```json
{
  "query": "DeepSeek V3.2 API pricing",
  "search_type": "web" // "web" | "x" | "both"
}
```

- Использует `x-ai/grok-4.1-fast:free` (бесплатно!)
- Auto-вызов `web_search` и `x_search`
- Возвращает summary + citations

### 🔧 Рефакторинг: Правильный GardenScript

**Было (спагетти, 17 файлов в 7 папках):**

```
src/
├── bloom/    (2 файла)
├── oak/      (2 файла)
├── ivy/      (3 файла)
├── vine/     (2 файла)
├── fern/     (2 файла)
├── rose/     (2 файла)
├── types/    (3 файла)
└── index.ts
```

**Стало (5 файлов, 0 папок):**

```
src/
├── types.ts         # Все типы
├── oak_core.ts      # Oak + Vine + Fern (бизнес-логика + HTTP + утилиты)
├── ivy_protocol.ts  # MCP tools + handlers
├── rose_format.ts   # Форматирование
└── index.ts         # Entry point
```

**Философия исправлена:**

- Категории через **нейминг внутри файлов** (`Oak_Generate`, `Vine_CallOpenRouter`)
- Файлы **жирные но навигируемые**
- Секции через комментарии: `// ============ OAK ============`

### 📋 Текущие команды (6 штук)

| Команда               | Назначение                            |
| --------------------- | ------------------------------------- |
| `generate_artifact`   | Генерация через LLM                   |
| `batch_generate`      | Mini-MoE: один промпт → много моделей |
| `generate_with_files` | Генерация с файлами в контексте       |
| `list_models`         | Список моделей OpenRouter             |
| `get_key_status`      | Статус API ключа                      |
| `grok_search`         | **NEW** — Поиск через Grok RAG        |

### 🐛 Исправления

- `grok_search`: slug изменён на `x-ai/grok-4.1-fast:free` (бесплатный)
- Удалены старые папки после рефакторинга

---

## v2.0.0 (December 2, 2025)

### Что изменилось с v1

| v1                                   | v2                       |
| ------------------------------------ | ------------------------ |
| Монолитный `server_entry.ts`         | Модульная структура      |
| `save: true/false`                   | Просто `output_path`     |
| `list_artifacts`, `cleanup_dropzone` | ❌ Убраны                |
| Только одиночная генерация           | ✅ `batch_generate`      |
| Нельзя приложить файлы               | ✅ `generate_with_files` |
| Модели захардкожены                  | ✅ `list_models` из API  |

### Entry point

`dist/index.js`

### Конфиг

```json
{
  "mcp-factory": {
    "type": "stdio",
    "command": "node",
    "args": [
      "c:/Users/Public/LilyCastle/15-Рабочий-Кабинет/MCP-Factory/dist/index.js"
    ],
    "env": {
      "OPENROUTER_API_KEY": "sk-or-v1-..."
    }
  }
}
```
