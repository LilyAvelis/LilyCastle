# 🏭 MCP-Factory

## 1. Что это

MCP-Factory — MCP-сервер для делегирования генерации дешёвым LLM с удобным интерфейсом, поддержкой batch, файлов и расширенным поиском через Grok RAG.

---

## 2. Установка

```bash
npm install
npx tsc
```

Создайте конфиг `mcp.json` (пример ниже):

```json
{
  "mcp-factory": {
    "type": "stdio",
    "command": "node",
    "args": ["dist/index.js"],
    "env": {
      "OPENROUTER_API_KEY": "sk-your-key"
    }
  }
}
```

---

## 3. Команды

| Команда               | Назначение                         |
| --------------------- | ---------------------------------- |
| `generate_artifact`   | Генерация через LLM                |
| `batch_generate`      | Mini-MoE: 1 промпт → много моделей |
| `generate_with_files` | Генерация с файлами в контексте    |
| `list_models`         | Список моделей OpenRouter          |
| `get_key_status`      | Статус API ключа                   |
| `grok_search`         | Поиск через Grok RAG               |

---

## 4. Быстрый старт

Пример запуска команды `grok_search`:

```json
{
  "command": "grok_search",
  "params": {
    "query": "DeepSeek V3.2 API pricing",
    "search_type": "web"
  }
}
```

Ожидаемый ответ:

```json
{
  "summary": "...",
  "citations": ["url1", "url2"]
}
```

---

## 5. Архитектура

Файлы и модули следуют GardenScript с неймингом:

- **Oak** — бизнес-логика и HTTP (включено Vine + Fern)
- **Vine** — OpenRouter вызовы
- **Fern** — утилиты и вспомогательные фичи
- **Ivy** — MCP tools и обработчики
- **Rose** — форматирование вывода

Все логические категории разбиты внутри файлов через нейминг функций и комментарии.

---

## 6. Модели

Для удобного выбора и информации по моделям используйте файл:

`@O/Rangs/5-diamond.md`

---

_Документация создана на основе версии MCP-Factory v2.1.0_
