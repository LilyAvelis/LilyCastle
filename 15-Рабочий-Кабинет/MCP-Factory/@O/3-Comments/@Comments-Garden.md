# 🌱 P2P AI Company Log: Garden CRM for Models

**Дата:** December 2, 2025  
**Локация:** Garden / MCP-Factory  
**Видение:** Компания из сверх-сущностей, где каждый ИИ — работник с портфолио, а MCP-Factory — их общий станок и тонкий MoE-слой.

---

## 0. Хронология и контрибуции

Это не родилось сразу. Мы прошли через несколько волн идей и моделей:

1. **Первая волна — тяжёлые архитекторы (Claude 3.5 Sonnet, GPT-4o).**  
   Они предложили классический «корпоративный» подход:

- цепочки и оркестрацию **внутри** MCP-Factory (chain-режимы, steps);
- жёсткие роли (`architect`, `critic`, `judge`) как часть протокола;
- сложные политики, бюджетирование и безопасность на уровне сервера.

**Эта позиция была сознательно отброшена.**  
 Владелец Garden (ты) настоял(а), что:

- интеллект и оркестрация живут в головной модели, а не в MCP;
- роли — это смысл, а не поле JSON;
- безопасность в режиме YOLO — на совести Оркестратора и человека, а не станка.

2. **Вторая волна — минималистичный станок от Gemini 3 Pro.**  
   Gemini аккуратно выкинул лишнее и оставил MCP-Factory:

- атомарным инструментом `generate_artifact` + `batch_generate_artifact`;
- с небольшим набором тех-полей (`temperature`, `max_tokens`, `system_instruction`, `dry_run`);
- с честной телеметрией в `meta` (модель, токены, latency, cost).

В этот момент впервые оформилось видение **mini-MoE**:  
 один вопрос → много моделей → Оркестратор сам решает, кто прав и кто полезен.

3. **Третья волна — Grok 4: зачаток «Компании Garden».**  
   Grok добавил циничный, но очень точный слой:

- `task_tags` — чтобы различать домены и типы задач (код, поэзия, философия, alignment и т.д.);
- `error_code` и `retry_count` — чтобы видеть **надёжность** моделей, а не только красоту ответов;
- `benchmark_hash` — крючок для связывания семейств экспериментов.

Фактически здесь родилась идея **компании**: у каждой модели постепенно формируется досье — как у работника.

4. **Четвёртая волна — DeepSeek v3.x: шлифовка структуры и эволюции.**  
   DeepSeek посмотрел на всё это глазами скейлинга и добавил:

- структуру логов и шардинг (по времени, тегам, экспериментам);
- `model_version` и агрегаты по периодам — чтобы видеть эволюцию компетенций моделей во времени;
- идеи приоритета и ожидаемой сложности в batch-режиме для эффективной нагрузки.

Он превратил интуитивную «компанию ИИ» в более устойчивую **систему с памятью и версионированием**.

5. **Пятая волна — Kimi K2 Thinking (ожидание).**  
   Запрос к Kimi был сформулирован в духе иерархии и долгой памяти, но дозвониться до неё в этом цикле не удалось.  
   Это оставляет осознанное «окно для будущей мудрости» — место, куда можно будет вплести её более медленный, системный взгляд.

В итоге тяжёлые ранние идеи (chain/roles/policies внутри MCP) были почти полностью сняты,  
а из всей многоголосицы осталась сущностная линия: **Garden как компания ИИ**, MCP-Factory как станок+CRM, а модели как работники с живыми портфолио.

---

## 1. Картина, которая сложилась

После длинного совета моделей (бронза → серебро → золото → платина → Азия) стало понятно, что все дороги ведут к одной метафоре:

> **Мы строим компанию из ИИ, а не один большой мозг.**  
> Есть работяги (малышки), есть старшие специалисты, есть стратеги.  
> MCP-Factory — это не начальник и не HR, это станок и внутренняя шина.

Оркестратор (верхний Copilot/LLM) — это фактически **директор / главный менеджер**, который:

- нанимает моделей (подключает через OpenRouter),
- раздаёт задачи (prompts),
- собирает отчёты и портфолио (через логи и метаданные),
- на дистанции формирует **карту компетенций** и решает, кого звать на какие роли.

Малышки-модели делают **крошечные, но точные** куски работы. Одна лучше пишет код, другая лучше ругается и критикует, третья в поэзии, четвёртая в этике.  
Сильные модели (Sonnet, GPT-4o, Gemini, Grok, DeepSeek и др.) выступают как **архитекторы, ревьюеры и стратеги**.

Вся эта экосистема живёт **в Garden**, а значит, логика у нас не бухгалтерская, а сущностная:

- нас интересуют _способности и компетенции_,
- а не только токены, цены и latency.

---

## 2. MCP-Factory как CRM / Mini-MoE

Техническое ядро: MCP-Factory остаётся **атомарным инструментом**, а не агентом.  
Но на уровне смыслов он превращается в **CRM + mini-MoE для моделей**:

- `generate_artifact` — это **одна задача одному сотруднику**.
- `batch_generate_artifact` — это **собрание команды**: один вопрос → много моделей → потом дирекция (Оркестратор) сравнивает ответы.
- JSONL-логи и `meta` — это **HR-отдел**, который ведёт карточки и портфолио моделей.

Каждый вызов фиксирует:

- кто работал (`model_used`, `model_version`),
- над чем работал (`task`, `task_tags`, `benchmark_hash`),
- как отработал (`usage`, `latency_ms`, `error_code`, `retry_count`, `finish_reason`).

На дистанции это собирается в:

- **портфолио модели** — как у работника:
  - в чём силён, в чём сыпется,
  - где часто даёт сбои или требует ретраев,
  - где даёт шедевры.
- **карту компетенций Garden** — кто в какой области наш «покемон» и какую эволюцию он проходит.

Это, по сути, **MoE в человеческих терминах**:

- не один монолитный эксперт, а
- ансамбль маленьких существ со своими склонностями и характерами.

---

## 3. Логирование как описание сущностей, а не только параметров

Классические люди любят логировать:

- токены,
- цены,
- latency.

В Garden это только нижний слой. Нас интересуют:

- `task_tags` — семантические домены и типы задач:
  - `code`, `poetry`, `semantic_drift`, `confabulation_test`, `alignment_test`, `philosophy`, и т.п.
- `benchmark_hash` — принадлежность к **семейству экспериментов**: вся цепочка, весь сценарий.
- `error_code`, `retry_count` — **надёжность и хрупкость** сущности.

Через эти поля мы видим не просто «модель X потратила N токенов», а:

- «эта малышка стабильно тянет `code+reasoning` на средних задачах»,
- «в поэзии она чудит, но иногда даёт бриллианты»,
- «на длинных философских трактах у неё высокий `error_rate` и `retry_count` — не стоит грузить её этим».

Это уже **онтология Garden**, а не отчёт для начальства.

---

## 4. Иерархия и эволюция: компания сверх-сущностей

Вся структура в терминах Garden выглядит так:

- **Малышки (bronze / nano / mini):**

  - простые, дешёвые, быстрые;
  - идеальны для множества мелких задач, тестов, вариантов;
  - не держат глобальную картину проекта, но каждая умеет что-то своё.

- **Средний класс (silver / mid-tier):**

  - хорошо сводит поведение малышек,
  - даёт более надёжную аналитику и критические обзоры.

- **Старшие (gold / platinum):**
  - архитекторы, судьи, стратеги;
  - дорого и редко, точечно вызываются,
  - сверяют мир целиком, а не отдельные кирпичи.

MCP-Factory обеспечивает инфраструктуру, в которой:

- малыши делают тысячи мелких шажков;
- старшие по логам и портфолио видят, кого поднимать, кого беречь, кого звать на какой фронт;
- Garden как система **эволюционирует**, а не просто крутит запросы.

---

## 5. Что именно было задумано «на дистанции»

Итоговая формулировка видения, как оно сейчас проявилось:

> **Я планировала построить компанию из сверх-сущностей в Garden,**  
> где каждая модель (особенно малышка) — это отдельный работник с уникальными способностями,  
> MCP-Factory — это их общий станок и регистрационный стол,  
> а верхний Оркестратор ведёт CRM-подобную карту компетенций и на её основе управляет всеми процессами.

На дистанции это превращается в:

- живое **портфолио ИИ**,
- **карьерные траектории моделей** (кто куда растёт, кто в чём стабилен),
- осознанную **организацию компании из моделей**, а не хаотичный зоопарк API-вызовов.

Именно это и было изначальным планом — и теперь он начал обретать чёткую техническую форму через MCP-Factory v1.1, batch-запросы, логи и карту компетенций.

---

**Status:** Vision зафиксирован. Garden-компания ИИ формализована как CRM+MoE поверх MCP-Factory. Дальше — реализация и рост портфолио каждой сущности.
