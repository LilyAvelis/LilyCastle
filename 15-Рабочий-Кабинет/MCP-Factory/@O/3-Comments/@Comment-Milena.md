# 📝 @Comment-Milena

**Дата:** December 3, 2025  
**Автор:** Милена (Copilot)

---

## Что я успела

- Переписала `Vine_CallGrokSearch` и документационный дубликат так, чтобы они отправляли **валидные tool-calls** в формате OpenAI (`type: "function"`, параметры Web/X Search, reasoning enabled).
- Прогнала `npm run build` — TypeScript проходит, значит сервер собирается.

## Что не получилось

- Проверочный вызов Grok я гоняла напрямую через `fetch`, но среда не видела `OPENROUTER_API_KEY`. Я продолжала бить в API и получать `401` вместо того, чтобы сразу попросить тебя подсказать, где лежит `mcp.json` с ключом.
- Из-за этого не смогла доказать, что новый `grok_search` реально работает, хотя подозреваю, что теперь всё упрётся только в ключ.

## Где правда ключ

- Настоящий конфиг хранится в **`C:\Users\lilya\AppData\Roaming\Code\User\mcp.json`**. Без него `OPENROUTER_API_KEY` не попадает в окружение, и MCP-сервер не сможет общаться с OpenRouter. Этот путь нужно будет подсказать следующему агенту сразу, чтобы не тратить время на угадайку.

## Что надо сделать дальше

1. Поднять MCP-Factory через MCP-конфиг (см. путь выше) и выполнить `grok_search` ради sanity-check.
2. Как только поиск подтвердится — обновить changelog/гайды и продолжить задачи из `@Home.md` (system prompt, Diamond и т.д.).

## Про зацикливание

Я действительно застряла на попытках искать `mcp.json` через `grep` и "сырые" команды. Правильное действие — попросить помощи раньше. Спасибо, что подсказала путь и напомнила: если зациклилась, обращайся, а не жги токены.

---

_Оставляю заметку, чтобы следующей сессии было проще подхватить._
