# 🌱 GardenScript — Философия и нейминг

## Часть 1: GardenScript в этом проекте

### Идея

GardenScript — это архитектурный подход, где код организован по **семантическим категориям**, а не по техническим слоям. Вместо `controllers/`, `services/`, `utils/` — папки с именами, которые отражают **роль** кода в системе.

Полная реализация GardenScript предполагает `.gs` файлы и компилятор `gsc`, но они ещё не готовы. Однако **философия нейминга** уже работает и применяется в этом проекте.

### Как это реализовано в MCP-Factory

Структура `src/` разделена по категориям из `garden.config.yaml`:

```
src/
├── bloom/    # 🌸 Инициализация — config, server setup
├── oak/      # 🌳 Бизнес-логика — generate, batch, models
├── vine/     # 🍇 Внешние HTTP — вызовы OpenRouter API
├── ivy/      # 🌿 MCP-протокол — tools, handlers
├── fern/     # 🌾 Утилиты — file I/O, filtering
├── rose/     # 🌹 Форматирование — вывод для пользователя
├── types/    # Типы TypeScript (вне таксономии)
└── index.ts  # Entry point
```

### Таксономия (garden.config.yaml)

```yaml
garden:
  taxonomy:
    - Bloom # Инициализация
    - Oak # Бизнес-логика
    - Vine # Внешние HTTP-вызовы
    - Ivy # MCP-протокол
    - Fern # Утилиты
    - Rose # Форматирование вывода
```

### Правила

1. **Категории фиксированы** — новых категорий быть не может
2. **Каждая папка = одна категория** — код живёт там, где его роль
3. **Нейминг внутри файлов** — функции/классы могут иметь префикс категории (`Bloom_init`, `Oak_generate`), но это опционально

### Зачем это нужно

- При чтении кода сразу понятно, **что делает** файл по его расположению
- Нет путаницы "это helper или service?"
- Архитектура **самодокументируется**

---

## Часть 2: Нейминг файлов (для других проектов)

> ⚠️ Этот раздел **не относится к MCP-Factory**, но важен для других проектов Garden.

### Проблема

Когда в проекте 20+ файлов одного типа (например, `.py`), они сортируются по алфавиту и превращаются в кашу:

```
analyze.py
clean.py
data.py
export.py
fetch.py
...
```

Невозможно быстро найти нужное. Где тут визуализация? Где утилиты?

### Решение: Категорийные префиксы

Каждый файл получает префикс категории:

```
analysis_rays.py
analysis_stats.py
data_export.py
data_fetch.py
helper_logger.py
helper_parser.py
visualization_heatmap.py
visualization_3d.py
```

Теперь файлы группируются по смыслу при сортировке.

### Типичные категории для Python-проектов

| Префикс          | Назначение                             |
| ---------------- | -------------------------------------- |
| `data_`          | Генерация, загрузка, сохранение данных |
| `analysis_`      | Вычисления, статистика, анализ         |
| `visualization_` | Графики, карты, изображения            |
| `control_`       | Проверки, тесты, валидация             |
| `ui_`            | CLI/GUI интерфейсы                     |
| `helper_`        | Общие утилиты                          |

### Отличие от GardenScript

| Аспект          | GardenScript                 | Файловый нейминг         |
| --------------- | ---------------------------- | ------------------------ |
| Категории       | Строго фиксированы в конфиге | Гибкие, по ситуации      |
| Где применяется | Папки = категории            | Префиксы в именах файлов |
| Масштаб         | Архитектура всего проекта    | Организация одной папки  |

Оба подхода решают одну задачу — **навигация по смыслу, а не по алфавиту**.
