# 🏭 MCP-Factory — Гайд по использованию (обновлённый)

## Что это такое?

MCP-Factory — это "контрабандный" MCP-сервер с оптимизацией для дешёвых LLM через OpenRouter. Идея: делегировать генерацию кода дешёвым моделям, чтобы сэкономить токены Copilot (Claude).

**Схема:**  
Copilot → MCP-Factory → OpenRouter → Gemma/Llama (бесплатные) → результат

---

## 🧰 Команды MCP-Factory

### ✅ `generate_artifact`

Генерация кода/текста через LLM с поддержкой файлов

**Параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `task` | string | Промпт для LLM |
| `model` | string | ID модели (по умолчанию: `qwen/qwen3-8b`) |
| `output_path` | string | Путь для сохранения |
| `temperature` | number | Креативность (0.0–2.0, по умолчанию: 0.7) |
| `max_tokens` | number | Макс. токенов (по умолчанию: 4096) |
| `system` | string | Системный промпт |

**Пример:**

```json
{
  "task": "Напиши функцию debounce на TypeScript",
  "model": "google/gemma-3-4b-it:free",
  "output_path": "C:/projects/debounce.ts"
}
```

---

### ✅ `batch_generate`

Мини-MoE: один промпт → много моделей

**Параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `task` | string | Общий промпт |
| `models` | array | Массив ID моделей |
| `temperature` | number | Креативность для всех |
| `max_tokens` | number | Лимит для всех |
| `system` | string | Системный промпт |

**Пример:**

```json
{
  "task": "Создай REST API на Node.js",
  "models": [
    "qwen/qwen3-8b",
    "google/gemma-3-4b-it:free",
    "meta-llama/llama-3.1-8b-instruct:free"
  ]
}
```

---

### ✅ `generate_with_files`

Генерация с контекстом из файлов

**Параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `task` | string | Что сделать с файлами |
| `files` | array | Массив путей к файлам |
| `model` | string | ID модели |
| `output_path` | string | Путь для сохранения |
| `temperature` | number | Креативность |
| `system` | string | Системный промпт |

**Пример:**

```json
{
  "task": "Найди баги в коде",
  "files": ["C:/projects/utils.ts", "C:/projects/api.ts"],
  "model": "anthropic/claude-3.5-haiku"
}
```

---

### ✅ `list_models`

Список моделей OpenRouter (фильтры: free/cheap/all)

**Параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `filter` | string | "free" / "cheap" / "all" |
| `search` | string | Поиск по имени |

**Пример:**

```json
{
  "filter": "free",
  "search": "qwen"
}
```

---

### ✅ `get_key_status`

Проверка статуса OpenRouter API ключа

**Пример:**

```json
{}
```

**Результат:**

```json
{
  "balance": "$100.00",
  "rate_limits": "10M tokens/day"
}
```

---

### ✅ `grok_search` (v2.1.0)

Поиск через Grok с RAG (web_search + x_search). Использует бесплатный `x-ai/grok-4.1-fast:free`

**Параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| `query` | string | Запрос (будьте конкретны!) |
| `search_type` | string | "web" / "x" / "both" (default: both) |
| `max_tokens` | number | Макс. токенов (default: 1024) |

**Пример:**

```json
{
  "query": "DeepSeek V3.2 API pricing",
  "search_type": "web"
}
```

**Результат:**

```json
{
  "summary": "DeepSeek V3.2 стоит $0.001/1K токенов...",
  "citations": ["https://deepseek.com/pricing", "https://openrouter.ai/docs"]
}
```

---

## 📁 Новая структура `src/` (v2.0.0)

**5 файлов вместо 17:**

```
src/
├── types.ts         # Все типы
├── oak_core.ts      # Oak + Vine + Fern (бизнес-логика + HTTP + утилиты)
├── ivy_protocol.ts  # MCP tools + handlers
├── rose_format.ts   # Форматирование
└── index.ts         # Entry point
```

**GardenScript нейминг:**

- **Oak** = Бизнес-логика (generate, batch, files)
- **Vine** = HTTP-запросы (OpenRouter)
- **Fern** = Утилиты (файловые операции)
- **Ivy** = MCP-протокол (команды)
- **Rose** = Форматирование вывода

---

## 📚 Выбор модели: Diamond

Для выбора модели см. **`@O/Rangs/5-diamond.md`** — там актуальные рекомендации по категориям:

| Категория   | Цена (Input/1M) | Рекомендация                  |
| ----------- | --------------- | ----------------------------- |
| 🆓 Copper   | $0              | `x-ai/grok-4.1-fast:free`     |
| 🥉 Bronze   | < $0.2          | `qwen/qwen3-8b-instruct`      |
| 🥈 Silver   | $0.2–1.0        | `deepseek-ai/deepseek-v3.2`   |
| 🥇 Gold     | $1.0–5.0        | `anthropic/claude-4.5-sonnet` |
| 💎 Platinum | $5.0+           | `anthropic/claude-opus-4.5`   |

**Принцип:** Bronze/Silver для рутины, Gold экономно, Platinum — редко.

---

## 🚀 Entry Point

**`dist/index.js`** — основной файл для запуска

**Конфиг:**

```json
{
  "mcp-factory": {
    "type": "stdio",
    "command": "node",
    "args": [
      "c:/Users/Public/LilyCastle/15-Рабочий-Кабинет/MCP-Factory/dist/index.js"
    ],
    "env": {
      "OPENROUTER_API_KEY": "sk-or-v1-..."
    }
  }
}
```

---

## 🚫 Удалённые функции

- `save: true/false` → заменён на `output_path`
- `list_artifacts`, `cleanup_dropzone` → убраны
- `server_entry.ts` → заменён на `index.ts`

---

## 📌 Обновление

```powershell
cd "c:\Users\Public\LilyCastle\15-Рабочий-Кабинет\MCP-Factory"
npx tsc
```

Перезагрузите VS Code (`Ctrl+Shift+P` → "Reload Window")
