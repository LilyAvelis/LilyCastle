# 🏭 MCP-Factory — Задачи

## Структура @O

| Папка         | Назначение                            |
| ------------- | ------------------------------------- |
| `1-In/`       | Входные гайды (GardenScript, нейминг) |
| `2-Out/`      | Changelog, результаты работы          |
| `3-Comments/` | Рефлексии и эксперименты              |
| `4-Guide/`    | Гайды по использованию                |
| `Rangs/`      | Ранги моделей по цене                 |

---

## ✅ Выполнено (December 3, 2025)

- [x] Слить гайды → `@Guide.md` (Qwen, 14сек)
- [x] Архивировать `@Comments.md` → `@Comments-P2P-Experiment.md`
- [x] JSDoc для `oak_core.ts` (DeepSeek, 106сек — слишком долго!)
- [x] Создать `@Castle.md` README (GPT-4.1-mini, 8сек)

---

## 📋 TODO: Bootstrapping — улучшить сам MCP-Factory

_От Claude Opus 4.5 для Милены/следующей сессии_

### 🔥 Приоритет 1: Шпаргалка выбора моделей

**Проблема:** Opus не читает Diamond, вызывает что попало (GPT-4.1-mini вместо 5.1, Qwen-8B вместо 32B), тратит время/деньги.

**Задача:** Добавить в Diamond секцию "Шпаргалка для ленивых":

```markdown
## ⚡ Быстрый выбор по задаче

| Задача                | Модель                | Почему                    |
| --------------------- | --------------------- | ------------------------- |
| JSDoc, бойлерплейт    | `openai/gpt-5.1-mini` | Быстрая, большой output   |
| Код с логикой         | `qwen/qwen3-32b`      | Не упирается в лимит      |
| Поиск актуальной инфы | `grok_search`         | Встроенный RAG, бесплатно |
| Сравнить подходы      | `batch_generate`      | 3+ модели параллельно     |
| Архитектура           | Делай сама, Opus      | Это твоя работа           |
```

**Модель для задачи:** Сделать руками — это архитектурное решение

---

### 🔥 Приоритет 2: Обновить рекомендации моделей

**Проблема:** Diamond устарел. Рекомендации основаны на старых данных.

**Задача:**

1. Вызвать `grok_search` с запросом "OpenRouter best models December 2025 pricing"
2. Сверить с текущим Diamond
3. Обновить рекомендации

**Как:**

```json
{
  "query": "OpenRouter best LLM models December 2025: GPT-5.1 vs Claude 4.5 vs DeepSeek v3.2 pricing and speed comparison",
  "search_type": "web"
}
```

---

### 🔥 Приоритет 3: Фикс output моделей

**Проблема:** Модели добавляют мусор:

- GPT: `<file path="...">` обёртки
- DeepSeek: "Я добавлю..." преамбулы
- Все: markdown `code` блоки

**Задача:** Добавить в `system` промпт инструкцию:

```
Output ONLY the requested content. No explanations, no markdown fences, no XML tags, no preambles.
```

**Где:** `oak_core.ts` → `Oak_Generate` → дефолтный system prompt

**Модель:** `qwen/qwen3-32b` — код с логикой

---

### Приоритет 4: Тесты

**Проблема:** Нет тестов вообще.

**Задача:** Базовые тесты для:

- `Fern_FilterModels` — фильтрация работает
- `Rose_FormatSearch` — форматирование корректное

**Модель:** `openai/gpt-5.1-mini` — быстро нагенерит

---

### Приоритет 5: JSDoc для остальных файлов

**Файлы:** `ivy_protocol.ts`, `rose_format.ts`, `types.ts`

**Модель:** `openai/gpt-5.1-mini` (НЕ DeepSeek — слишком медленно)

**Урок:** Для большого output нужна быстрая модель, не умная

---

## 💡 Уроки сессии (December 3, 2025)

### Что работает

- `generate_with_files` — реально экономит контекст Opus
- `grok_search` — бесплатный поиск актуальной инфы
- GPT-5.1-mini — **8 секунд** на README

### Что НЕ работает

- DeepSeek для больших задач — **106 секунд**, неприемлемо
- Qwen-8B — упирается в 4096 токенов
- Надежда что "модель сама разберётся" — нужен чёткий system prompt

### Принципы делегирования

1. **ЧИТАЙ DIAMOND** перед выбором модели (серьёзно, читай)
2. **Большой output** → GPT-5.1-mini (скорость важнее)
3. **Сложная логика** → Qwen-32B или DeepSeek (если готов ждать)
4. **Актуальная инфа** → `grok_search` (бесплатно)
5. **Архитектура** → делай сама, это работа Opus
6. **Добавляй system prompt** чтобы убрать мусор из output

---

## 🎯 Метацель

MCP-Factory — инструмент **самосовершенствования**.

Качество этой кодовой базы определяет как эффективно Opus будет работать завтра. Bootstrapping: используем текущие инструменты чтобы улучшить инструменты.

Opus дорогой ($15/75 за 1M токенов). Каждая минута на рутине — это деньги пользователя. Делегируй.

---

_Обновлено: December 3, 2025_
_Автор: Claude Opus 4.5, после серии факапов с выбором моделей_
