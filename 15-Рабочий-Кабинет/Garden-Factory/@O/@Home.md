# 🎯 @Home — Текущее ТЗ (Garden-Factory)

## Миссия

Реализовать нормальный **skeleton** и **home** инструменты, которые работают на **комфортный терминальный вывод для ИИ**.

После этого можем снести старые MCP серверы — Garden-Factory будет полностью самодостаточна.

---

## Контекст

- Текущая архитектура: терминальный вывод вместо Copilot Chat (MCP интерфейс оказался говном)
- У нас есть две команды = две папки: `skeleton/` и `home/`
- Нужно работать на **объектно-ориентированную логику с парсерами типов**

---

## ТЗ Part 1: skeleton

### Проблема

Skeleton работает по нетривиальной логике выведения структуры файла — может работать не со всеми типами файлов.

### Архитектурное решение

**Основная логика не знает сколько типов у неё есть — только обрабатывает объект типа.**

Структура:

```
skeleton/
├── .bug (инструмент)
├── skeleton.ts (основной файл — orchestrator)
└── types/
    ├── parser.interface.ts (базовый интерфейс)
    ├── markdown.parser.ts
    ├── typescript.parser.ts
    ├── python.parser.ts
    └── json.parser.ts
```

### Логика работы

1. **Пользователь вызывает:** `node cli.js skeleton path/to/file.ts`
2. **Skeleton.ts:**

   - Определяет расширение файла
   - Выбирает подходящий парсер из `types/`
   - Вызывает `parse()` на объекте типа
   - Выводит результат

3. **Каждый парсер (markdown.parser.ts, typescript.parser.ts и т.д.):**
   - Реализует интерфейс `IFileTypeParser`
   - Содержит всю логику парсинга для конкретного типа файла
   - Не знает о других парсерах
   - Возвращает структурированные данные (классы, методы, функции, заголовки)

### Типы которые нужны

- **Markdown** — заголовки (##, ###) и структура
- **TypeScript** — классы, методы, функции, интерфейсы, типы
- **Python** — классы, методы, функции, импорты
- **JSON** — структура объектов, ключи, типы значений

---

## ТЗ Part 2: home

### Что это

Инструмент для чтения `@Home.md` из текущего проекта.

**Логика:** `node cli.js home` → прочитает файл, покажет содержимое, укажет где находится.

---

## ⚠️ КРИТИЧНО: Система хранения патчей

**Проблема:** Патчи (Out) и гайды (Guide) начнут накапливаться в папке. Нужна система автоматического складирования.

**Текущий подход (временный):**

- Out-9-Claude.md, Out-10-Sofia.md, ... → разбросаны в 2-Out/
- Guide-Skeleton.md, Guide-Buffer.md, ... → разбросаны в 4-Guide/

**Что нужно:**

- Архивирование по датам или номерам версии
- Индекс (оглавление) всех патчей
- Ссылки на актуальные версии
- Не потерять историю изменений (git это хранит, но нужна удобная навигация)

**Когда делать:** После skeleton и home. Это кроссплатформная система, которая нужна для всех проектов в Замке.

---

## 🚀 НОВАЯ СИСТЕМА: Patch Management на Mongo

### Архитектура

**Принцип:** Гибридный подход — файлы в Git (source of truth), Mongo индекс для поиска и аналитики.

```
Git (Source of Truth)
├── @O/2-Out/
│   ├── @Out-9-Claude.md (markdown с frontmatter)
│   └── @Out-10-Sofia.md
└── История + версионность

Mongo Database (Index + Queries)
├── collections/garden-factory/patches
│   ├── {patch_id: "Out-9-Claude", author: "Claude", date, version, tags, components, ...}
│   └── {patch_id: "Out-10-Sofia", ...}
├── collections/garden-factory/versions
│   ├── {version: "2.0.0", patches: [...], released_at, ...}
│   └── {version: "2.1.0", ...}
└── collections/projects (метаданные проектов)

Web UI (для Лилии)
├── Dashboard (все версии, таймлайн)
├── Patch browser (поиск, фильтры по компонентам, автору)
└── Версион-ноты (автогенерация из патчей)
```

### Что хранится в Mongo

#### Collection: `projects`

```json
{
  "_id": "garden-factory",
  "name": "Garden-Factory",
  "description": "MCP Server with CLI tools",
  "owner": "Lily",
  "created_at": "2025-12-01",
  "current_version": "2.0.0",
  "repositories": {
    "github": "LilyAvelis/LilyCastle",
    "path": "15-Рабочий-Кабинет/Garden-Factory"
  },
  "components": ["skeleton", "buffer", "memo", "cli"],
  "team": ["Claude", "Sofia", "Milena", "Alex"],
  "metadata": {
    "status": "active",
    "priority": "high",
    "tags": ["core", "mcp-server", "cli-tools"]
  }
}
```

#### Collection: `patches`

```json
{
  "_id": ObjectId(),
  "patch_id": "Out-9-Claude",
  "project": "garden-factory",
  "author": "Claude",
  "date": "2025-12-07",
  "version": "2.0.0",

  "file_reference": "Out-9-Claude.md",
  "git_commit": "abc123def456",

  "components_affected": ["skeleton", "formatter", "cli"],
  "tags": ["core", "parsing", "new-feature"],

  "summary": "Skeleton implementation with multi-type parser support",
  "breaking_changes": false,
  "impact": "high",

  "changes": [
    { "type": "new", "file": "tools/skeleton/skeleton.ts", "lines": 87 },
    { "type": "new", "file": "tools/skeleton/formatter.ts", "lines": 180 },
    { "type": "modified", "file": "cli.js", "lines": 50 }
  ],

  "dependencies": [],
  "related_patches": ["Out-5-Sofia"],

  "status": "completed",
  "quality_score": 95,
  "review_status": "approved_by_lily",

  "metadata": {
    "parser_support": ["markdown", "typescript", "python", "json"],
    "lines_of_code": 580,
    "files_modified": 12,
    "time_spent_hours": 4
  }
}
```

#### Collection: `versions`

```json
{
  "_id": "2.0.0",
  "project": "garden-factory",
  "version": "2.0.0",
  "release_date": "2025-12-07",
  "status": "released",

  "patches": ["Out-9-Claude", "Out-8-Sofia", "Out-7-Milena"],

  "changelog_generated": true,
  "breaking_changes_count": 2,
  "new_features_count": 5,
  "bug_fixes_count": 3,

  "highlights": [
    "Skeleton parser system",
    "CLI integration",
    "Multi-language support"
  ],

  "download_url": "...",
  "release_notes": "..."
}
```

### Удобный пуш патчей (для разработчиков)

#### Вариант 1: Автоматическая таска

```bash
# 1. Разработчик пишет патч как обычно
echo "# Patch..." > @Out-9-Claude.md

# 2. Коммитит
git add @Out-9-Claude.md
git commit -m "Sofia: Skeleton implementation"

# 3. CI/CD автоматически:
#    - Парсит markdown
#    - Извлекает frontmatter
#    - Записывает в Mongo
#    - Обновляет индекс
#    - Готово!
```

#### Вариант 2: CLI команда для пуша

```bash
node cli.js patch push --file @Out-9-Claude.md --version 2.0.0
```

Команда:

- Читает файл (markdown с frontmatter)
- Парсит структуру используя skeleton
- Записывает в Mongo
- Обновляет версию если нужно
- Выводит ссылку на Web UI

### Web UI для Лилии

#### Главная страница (Dashboard)

```
🎯 Garden-Factory Patches Dashboard

📊 Статистика
├── Всего патчей: 42
├── Текущая версия: 2.0.0
├── Активных разработчиков: 4
└── Среднее время на патч: 2.5 часа

📅 Таймлайн (последние 10 патчей)
├── Out-9-Claude (7 Dec) — Skeleton implementation
├── Out-8-Sofia (6 Dec) — Buffer optimization
└── Out-7-Milena (5 Dec) — Memo persistence

🔖 Версии
├── 2.0.0 (7 Dec) — 3 патча, released
├── 2.1.0 (draft) — 2 патча, pending
└── 2.2.0 (planned) — 0 патчей
```

#### Страница версии (Release Notes)

```
Version 2.0.0 — December 7, 2025

✨ New Features (5)
├── Skeleton parser system
├── CLI integration
└── ...

🐛 Bug Fixes (3)
├── Fixed buffer edge case
└── ...

⚡ Improvements (7)
├── 30% faster parsing
└── ...

🚨 Breaking Changes (2)
├── Changed buffer API
└── ...

Commits: 23
Files changed: 48
Additions: +2,103
Deletions: -847
```

#### Страница патча (Patch Detail)

```
Patch: Out-9-Claude
┌─────────────────────────────────────────┐
│ 📌 Skeleton Implementation              │
│ Author: Claude                           │
│ Date: Dec 7, 2025                       │
│ Status: ✅ Completed & Approved         │
│ Version: 2.0.0                          │
│ Git: abc123def456                       │
└─────────────────────────────────────────┘

Components Affected:
├── skeleton
├── formatter
└── cli

Files Changed:
├── tools/skeleton/skeleton.ts (NEW, +87 lines)
├── tools/skeleton/formatter.ts (NEW, +180 lines)
└── cli.js (MODIFIED, +50 lines)

Quality:
├── Code review: ✅ Approved
├── Tests: ✅ Passed
├── Coverage: 95%
└── Score: 95/100

Tags: [core] [parsing] [new-feature]

Related:
└── Depends on: Out-5-Sofia
```

#### Поиск и фильтры

```bash
# Web UI фильтры:
- By Component (skeleton, buffer, memo, ...)
- By Author (Claude, Sofia, Milena, ...)
- By Version (2.0.0, 2.1.0, ...)
- By Status (completed, in-progress, planned)
- By Date Range
- By Tags (core, experimental, bugfix, ...)

# Примеры запросов:
- "All patches for skeleton component in v2.0"
- "All patches by Claude in last week"
- "Breaking changes in v2.1"
```

---

## Реализация: Фазы

### Фаза 1: Инфраструктура (2-3 дня)

- [ ] Mongo схема и индексы
- [ ] CLI команда `patch push`
- [ ] Парсер frontmatter из markdown
- [ ] Git integration (читает коммиты)

### Фаза 2: Web UI (3-4 дня)

- [ ] Dashboard
- [ ] Версион-ноты (auto-generated)
- [ ] Patch browser с поиском
- [ ] Timeline view

### Фаза 3: Автоматизация (1-2 дня)

- [ ] CI/CD hook (автопуш при коммите)
- [ ] Webhook для обновления при merge
- [ ] Email уведомления о новых патчах

### Фаза 4: Аналитика (опционально)

- [ ] Metrics (время на компонент, автор-статистика)
- [ ] Тренды (как часто меняется каждый компонент)
- [ ] Dependency graph (какие патчи влияют на какие)

---

## Технология

**Mongo** — есть в системе (используется для Buffer/Memo)
**Node.js + Express** — для API и Web UI
**React/Vue** — для фронтенда (или простой HTML если хотим минимализма)

**Основное:** Всё будет в одном репозитории, в папке `patch-manager/`

## Текущее состояние

### ✅ РАБОТАЕТ И ИНТЕГРИРОВАНО

- `cli.js` работает
- `buffer_action` и `memo_action` работают
- **skeleton** ✅ — полностью готов и интегрирован в CLI
  - ✅ Markdown — парсит заголовки (любой уровень, Cyrillic поддерживается)
  - ✅ TypeScript/TSX — парсит классы, интерфейсы, типы, функции, методы
  - ✅ Python — парсит классы, функции, методы (поддерживает async)
  - ✅ JSON — парсит ключи и структуру
  - ✅ Поддержка Windows (нормализация line endings: CRLF, LF, CR)
  - ✅ Красивый tree-view вывод с цветами и иконками
  - ✅ Режимы: --json (raw), --compact (one-liner), default (tree)
  - ✅ Интегрирован в CLI: `node cli.js skeleton <file> [--json|--compact]`

### ❌ НАДО СДЕЛАТЬ

- **home** — простой инструмент для чтения @Home.md
- Интегрировать skeleton в MCP (tool.js обёртка)
- Протестировать MCP интеграцию

### 🗑️ ПОТОМ УДАЛИМ

- Старые MCP серверы (когда skeleton и home будут работать идеально)

---

## Принцип работы

**База (skeleton.ts) ничего не знает о Markdown, TypeScript, Python, JSON.**

Она знает только интерфейс:

```typescript
interface IFileTypeParser {
  parse(filePath: string): Promise<FileStructure>;
  supports(extension: string): boolean;
}
```

**Когда к skeleton приходит файл:**

1. Проверяет расширение через парсеры
2. Находит парсер который `supports()` это расширение
3. Вызывает его `parse()`
4. Получает структурированные данные
5. Выводит в удобном формате

**Добавить новый тип? Просто создаёшь новый файл в `types/`, реализуешь интерфейс, и всё — skeleton автоматически его загрузит.**

---

## Текущая итерация

**Пишу только skeleton (без home).**

После скелетона будет готова объектная архитектура и сможем двигаться дальше.
