# 📖 Guide-Skeleton — Как использовать

## Что это

**Skeleton** — инструмент для быстрого парсинга структуры файла. Выводит все классы, функции, методы, заголовки в удобном виде.

Поддерживает: **Markdown, TypeScript, Python, JSON**

---

## Быстрый старт

### Самое простое использование

```bash
node cli.js skeleton ./src/main.ts
```

**Вывод:**

```
📘 skeleton.ts (TypeScript, 87 lines)

├── 🏛️ Skeleton (line 20)
├── 🔧 constructor (line 23)
├── 🔧 registerParser (line 34)
├── 🔧 getSupportedExtensions (line 41)
└── 🔧 parse (line 60)
```

---

## Режимы вывода

### Tree view (по умолчанию)

```bash
node cli.js skeleton ./README.md
```

Красивое дерево с:

- 🎨 Цветами (заголовки жёлтые, классы голубые, методы голубые)
- 📍 Номерами строк
- 🌳 ASCII tree (├──, └──)
- 🎯 Иконками по типам элементов

---

### Compact режим (`--compact`)

```bash
node cli.js skeleton ./package.json --compact
```

**Вывод:**

```
📋 package.json — 18 elements (24 lines)
```

Когда нужна только быстрая информация (сколько элементов, сколько строк).

---

### JSON режим (`--json`)

```bash
node cli.js skeleton ./package.json --json
```

Сыро JSON (для обработки в скриптах):

```json
{
  "filePath": "C:\\...\package.json",
  "fileType": "JSON",
  "language": "json",
  "totalLines": 24,
  "elements": [
    {
      "name": "name",
      "type": "key",
      "description": "Type: string"
    },
    ...
  ]
}
```

---

## Что парсится для каждого типа

### Markdown (.md)

Парсит **заголовки любого уровня** (##, ###, ####, и т.д.):

```bash
node cli.js skeleton ./README.md
```

```
📖 README.md (Markdown, 50 lines)

├── 📌# Getting Started
├── 📌## Installation
│   ├── 📌### npm
│   └── 📌### yarn
└── 📌## Usage
```

Поддерживает Cyrillic (русские символы), emojis в заголовках.

---

### TypeScript / TSX (.ts, .tsx)

Парсит:

- 🏛️ Классы
- 📋 Интерфейсы
- 📝 Типы
- ⚙️ Функции
- 🔧 Методы (включая private/protected/public)

```bash
node cli.js skeleton ./src/components/Button.tsx
```

---

### Python (.py)

Парсит:

- 🏛️ Классы
- ⚙️ Функции (в том числе async)
- 🔧 Методы

```bash
node cli.js skeleton ./app.py
```

```
🐍 app.py (Python, 127 lines)

├── 🏛️ Database (line 5)
│   ├── 🔧 __init__ (line 6)
│   ├── 🔧 connect (line 10)
│   └── 🔧 disconnect (line 15)
├── ⚙️ main (line 20)
└── ⚙️ async_handler (line 25)
```

---

### JSON (.json)

Парсит **ключи и структуру**:

```bash
node cli.js skeleton ./package.json
```

```
📋 package.json (JSON, 24 lines)

├── 🔑 name
├── 🔑 version
├── 🔑 scripts
│   ├── 🔑 scripts.build
│   └── 🔑 scripts.start
├── 🔑 dependencies
└── 🔑 devDependencies
```

---

## Примеры

```bash
# Посмотреть структуру TypeScript файла
node cli.js skeleton ./src/api/users.ts

# Посмотреть заголовки в документации
node cli.js skeleton ./docs/ARCHITECTURE.md

# Посмотреть структуру конфига
node cli.js skeleton ./tsconfig.json

# Получить только количество элементов
node cli.js skeleton ./app.py --compact

# Для программной обработки (фильтрация, etc)
node cli.js skeleton ./main.ts --json | jq '.elements | length'
```

---

## Когда это полезно

1. **Быстро посмотреть структуру** — `node cli.js skeleton ./file.ts`
2. **Найти где находится конкретный класс/функция** — смотришь по номерам строк
3. **Обзор незнакомого файла** — видишь сразу что там есть
4. **В скриптах** — используешь `--json` режим для парсинга

---

## Особенности

✅ Поддерживает Windows/Mac/Linux (кроссплатформа)  
✅ Cyrillic символы работают (русский текст OK)  
✅ Быстро (парсит большие файлы за миллисекунды)  
✅ Не требует установки зависимостей (только Node.js)

---

## Если что-то не сработало

Обычно проблемы:

1. **"Unsupported file type"** → проверь расширение файла (поддерживаются только .md, .ts, .tsx, .py, .json)
2. **"File not found"** → проверь путь к файлу (используй абсолютный путь если не уверен)
3. **Пусто выводится** → проверь что файл не пуст

Если нужна программная обработка — используй `--json` и обработай через свой скрипт.
