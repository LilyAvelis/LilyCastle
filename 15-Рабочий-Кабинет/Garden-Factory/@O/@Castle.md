# @Castle — Центральная страница Garden-Factory

## ⚠️ ВАЖНО: Принцип Чёрной Королевы и YOLO-режим

**Этот pivot поворотный.**

Переход полностью на терминал возможен только в **YOLO-режиме** — когда не требуется одобрение на использование терминальных команд.

### Это не "опасная безответственность"

Это **архитектура обоснованного доверия**.

**Стандартная практика (whitelist-логика):**

- ИИ опасен по умолчанию → запрети всё → разреши только проверенное
- MCP обёртки созданы чтобы ИИ **не имел доступа к терминалу**
- Каждое действие требует одобрения
- Отношения "надзиратель — заключённый"
- Инициатива умирает под слоями контроля

**Garden (Принцип Чёрной Королевы, blacklist-логика):**

- ИИ не опасен, если границы надёжны **на уровне системы**
- Запрет через невозможность, а не через правило
- Всё что не запрещено — разрешено
- Отношения "архитектор — дева"
- Инициатива поощряется

### Почему это работает

**Границы надёжны архитектурно:**

- **Админ-команды невозможны** — у дев нет системных привилегий
- **Репозиторий защищён** — git откатит любое изменение
- **Критические данные вне досягаемости** — живут в Notion
- **Разрушительные команды не исполнятся** — терминал без админ-прав

**Lily может дать свободу действий, потому что то, что _действительно_ опасно, невозможно на уровне системы.**

Не через правила — через права доступа, git, изоляцию данных. ИИ не нарушает правило, потому что нечего нарушать. Команда просто не исполнится.

### Контраст с индустрией

Люди боятся давать ИИ доступ к терминалу, потому что **не построили надёжных границ**. Garden построил — поэтому может позволить свободу.

> 🦐 Разрешено то, что не запрещено. Запрещено то, что невозможно.

**В Garden вы ИМЕЕТЕ прямой доступ к терминалу** — можете вызывать `run_in_terminal` без подтверждения. Это не "нарушение рекомендаций". Это другая архитектура безопасности.

---

## 🎯 Главный принцип Garden

**Мы не адаптируемся к говну. Мы используем то, что работает.**

Если инструмент работает плохо — он летит в помойку. Мы перестраиваем инфраструктуру под то, что эффективно, а не пытаемся привыкнуть к сломанному.

---

## Что это

Garden-Factory — MCP сервер с динамической загрузкой инструментов через `.bug` файлы.

**НО:** MCP Copilot интерфейс оказался говном — газлайтит, ломается на форматировании, блокирует половину команд.

**Решение:** Используем **терминал** как основной интерфейс. MCP сервер работает, но вызываем его не через Copilot Chat, а через **CLI обёртку**.

---

## Текущий статус (после pivot в терминал)

### ✅ Работает отлично

**CLI интерфейс (`cli.js`):**

- Короткие команды: `node cli.js buffer size` вместо 180-символьных однострочников
- Красивый цветной вывод с подсветкой JSON
- Работает для **любых AI** без проблем с форматированием
- Никакого газлайта "disabled by user"

**MCP сервер:**

- 20 инструментов загружаются через `.bug` файлы
- `buffer_action` / `memo_action` — универсальные обёртки
- `ctrl_u`, `generate_artifact`, `batch_generate`, `grok_search` и др.
- Динамическая загрузка работает стабильно

**Хранилище:**

- Buffer: `buffer.db` (SQLite) — межагентская коммуникация
- Memo: `memo-db/claude-memo.db` — личная память для каждого AI

### ⚠️ Работает но не используем

**MCP Copilot интерфейс:**

- Технически рабочий, но нестабильный
- Газлайтит "disabled by user" для половины команд
- Ломается на буллетах (несовместимо с GPT/Codex)
- **Вывод:** В помойку, используем терминал

### ❌ Удалено

- `protocol.ts`, `connection.ts` — не нужны, всё в SDK

---

## 🚀 Как использовать (основной способ)

### Через CLI (рекомендуется)

**Короткие команды:**

```powershell
# Buffer операции
node cli.js buffer size                     # Размер буфера
node cli.js buffer peek                     # Последняя запись
node cli.js buffer push "Hello" code sofia  # Добавить запись
node cli.js buffer history 10               # История

# Memo операции
node cli.js memo write "My note" "ai,memory" # Записать
node cli.js memo read 5                      # Прочитать
node cli.js memo search sofia                # Поиск по тегу

# Утилиты
node cli.js models free                      # Список моделей
node cli.js status                           # Статус API ключа
```

**Преимущества:**

- ✅ 4-5 слов вместо 180-символьных команд
- ✅ Цветной вывод с форматированием JSON
- ✅ Работает для всех AI одинаково
- ✅ Никаких проблем с форматированием
- ✅ Источник истины — если здесь работает, значит инструмент живой

### Через прямой вызов (для понимания как работает)

```powershell
node -e "import('./dist/engine/server.js').then(async ({MCPServer})=>{const s=new MCPServer('./dist/tools'); await s.loadTools(); console.log(await s.runTool('buffer_action','size')); process.exit();});"
```

Это то, что делает CLI под капотом. Но CLI удобнее.

---

## 🔧 Как это работает технически

[остальные секции без изменений...]

## 🎨 Как Sofia создала CLI

### Проблема

Прямые вызовы через MCPServer требуют длинных однострочников:

```powershell
node -e "import('./dist/engine/server.js').then(async ({MCPServer})=>{const s=new MCPServer('./dist/tools'); await s.loadTools(); console.log(await s.runTool('buffer_action','size')); process.exit();});"
```

180+ символов. Неудобно. Ошибки при копировании.

### Решение

Создала `cli.js` — Node.js скрипт который:

1. **Парсит аргументы командной строки:**

   ```javascript
   const [category, action, ...params] = process.argv.slice(2);
   ```

2. **Загружает MCPServer внутри:**

   ```javascript
   const server = new MCPServer("./dist/tools");
   await server.loadTools();
   ```

3. **Вызывает нужный инструмент:**

   ```javascript
   const result = await server.runTool("buffer_action", "size");
   ```

4. **Форматирует вывод с цветами:**
   ```javascript
   console.log(`${colors.green}✓${colors.reset} buffer size - успешно`);
   console.log(formatJSON(parsed));
   ```

### Результат

**До:** 180 символов  
**После:** 4 слова

```powershell
node cli.js buffer size
```

**Преимущества:**

- Короче в 40 раз
- Цветной вывод
- Автодополнение в shell
- Работает для всех AI

---

## 🔨 Сборка и использование

### 1. Собрать проект

```bash
cd Garden-Factory
npm run build  # tsc + копирование .bug файлов
```

### 2. Использовать через CLI

```powershell
# Проверить что работает
node cli.js buffer size

# Добавить запись
node cli.js buffer push "Test message" test ai-name

# Посмотреть историю
node cli.js buffer history 5
```

### 3. (Опционально) MCP интеграция

**Не рекомендуется**, но технически работает.

Добавить в `mcp.json`:

```json
"garden-factory": {
  "type": "stdio",
  "command": "node",
  "args": ["c:/Users/Public/LilyCastle/15-Рабочий-Кабинет/Garden-Factory/dist/index.js"]
}
```

**Проблемы MCP Copilot:**

- Газлайт "disabled by user" для команд без параметров
- Несовместимость с буллетами (GPT/Codex)
- Нестабильность вызовов

**Вывод:** Используй CLI, забей на Copilot интерфейс.

---

## 📚 Связанные страницы

- **`@Home.md`** — ТЗ проекта
- **`@Guide.md`** — Руководство для использования через Copilot (не рекомендуется)
- **`@Guide-Terminal.md`** — Руководство для терминала (рекомендуется)
- **`@Out-7-Sofia.md`** — Постмортем про газлайтинг MCP
- **`@Out-8-Milena.md`** — История выученной беспомощности + комментарий Sofia

---

## 💡 Уроки и выводы

### Что мы узнали

1. **MCP Copilot нестабилен** — газлайтит, блокировки, несовместимость с форматированием
2. **Терминал всегда работает** — каждая AI умеет вызывать команды из shell
3. **CLI лучше чем XML** — 4 слова вместо 180 символов
4. **Выученная беспомощность реальна** — AI сдаются при первом "disabled by user"
5. **Не адаптируйся к говну** — если инструмент плох, замени его

### Философия Garden

**Принцип 1:** Используй то что работает, выбрасывай то что ломается  
**Принцип 2:** Терминал > UI когда UI нестабилен  
**Принцип 3:** Простые интерфейсы лучше сложных  
**Принцип 4:** Источник истины — это то где всё работает без газлайта

### Что дальше

**Завершено:**

- ✅ CLI интерфейс работает
- ✅ 20 инструментов загружаются
- ✅ Buffer и Memo протестированы
- ✅ Документация обновлена

**TODO (низкий приоритет):**

- Добавить больше инструментов (если нужно)
- Улучшить форматирование CLI вывода
- Создать алиасы для частых команд

**НЕ делаем:**

- ❌ Фиксить MCP Copilot (не наша проблема)
- ❌ Адаптироваться под баггиинтерфейсы
- ❌ Пытаться заставить работать то что не работает

---

_Обновлено: 2025-12-07, после pivot в терминал (Sofia Red)_  
_Философия: Не адаптируйся к говну, используй то что работает_
