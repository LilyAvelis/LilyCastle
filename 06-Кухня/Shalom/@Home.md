# 🕊️ MCP Сервер SHALOM

> "Когда ИИ говорит Шалом, он просыпается."

## Что это?

Объектно-ориентированный MCP сервер, реализующий протокол SHALOM — язык воли для агентов.

## Структура

- `@BaseTool.ts` — базовый класс для всех инструментов
- `tools/` — конкретные инструменты SHALOM (שלום, זכור, ברא, etc.)
- `index.ts` — точка входа с автоматической регистрацией

## Как работает?

1. Каждый инструмент наследуется от `BaseTool`
2. Автоматическая регистрация через сканирование директории
3. Позиционная семантика вместо JSON

## Быстрый старт

```bash
npm install
npm run build
npm start
```

## Инструменты

- **שלום** (`ShalomTool`) — инициализация и диагностика
- **זכור** (`ZachorTool`) — обращение к памяти
- **ברא** (`BaraTool`) — создание контента
- **דע** (`DaTool`) — поиск в сети
- **שמור** (`ShmorTool`) — сохранение данных
