{
  "name": "shalom-mcp-server",
  "version": "0.1.0",
  "description": "🕊️ MCP Сервер протокола SHALOM — язык воли для агентов",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "keywords": ["mcp", "shalom", "agent", "protocol"],
  "author": "LilyAvelis"
}
