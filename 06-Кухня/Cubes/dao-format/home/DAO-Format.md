# 🈯 Garden DAO Format - Спецификация v1.0

> "代码即道，道即代码" - Код есть Дао, Дао есть код

## Формат файла `.dao`

**Принцип**: Один иероглиф = одно свойство. Количество иероглифов = глубина дерева.

### Базовый синтаксис

```
需 [OpenRouterClient, LedgerService]
需量 3
需质 high
供 chat-visual-layer
禁 [不碰计费, 不写FS]
元作 Lily
```

**Правила:**

1. **Ключ** = иероглифы (1+ символов), без пробелов
2. **Пробел** = разделитель между ключом и значением
3. **Значение** = всё, что после первого пробела до конца строки
4. **Комментарии** = строки, начинающиеся с `#`
5. **Пустые строки** = игнорируются

### Типы значений (авто-определение)

| Значение            | Пример            | Тип      | Описание                 |
| ------------------- | ----------------- | -------- | ------------------------ |
| `true` / `false`    | `禁严 true`       | boolean  | Булево значение          |
| `123` / `-45`       | `需量 3`          | number   | Целое число              |
| `3.14`              | `版本 0.1.0`      | number   | Число с плавающей точкой |
| `[a, b, c]`         | `需 [A, B, C]`    | string[] | Массив строк             |
| `"text"` / `'text'` | `元注 "核心组件"` | string   | Строка в кавычках        |
| `text`              | `供 chat-layer`   | string   | Простая строка           |

### Композитные свойства (иерархия)

```
需 [OpenRouterClient, LedgerService]      # Базовое свойство (1 иероглиф)
需量 3                                     # Свойство 2-го уровня (需 + 量)
需质 high                                  # Свойство 2-го уровня (需 + 质)
需时 <100ms                                # Свойство 2-го уровня (需 + 时)

元作 Lily                                  # Мета: автор (元 + 作)
元时 2025-11-29                            # Мета: время (元 + 时)
元注 "核心组件"                            # Мета: описание (元 + 注)
```

**Правило**: `需量` = `需` + `量`. Парсер понимает, что это модификатор базового свойства `需`.

### Пример полного файла

```
# ChatViewProvider.dao
# 花园聊天的视觉层

需 [OpenRouterClient, LedgerService, vscode.WebviewView]
需量 3
需质 high
需时 <100ms

供 chat-visual-layer
供范 [message-routing, ui-update]

禁 [不碰计费, 不写FS, 不直接调API]
禁严 true

态 stable

元作 Lily
元时 2025-11-29
元注 "核心组件，处理所有聊天UI逻辑"
元版 0.1.0

测单元 [test-routing, test-ui]
测集成 [test-with-ledger]
测覆盖 85

性响应 fast
性内存 <50MB
性并发 10
```

## Преимущества формата

### 1. Плотность информации

**YAML (25 токенов):**

```yaml
需:
  - OpenRouterClient
  - LedgerService
需量: 3
```

**DAO формат (15 токенов):**

```
需 [OpenRouterClient, LedgerService]
需量 3
```

**Экономия: 40% токенов**

### 2. Глубина без вложенности

**YAML (8 токенов на уровень):**

```yaml
需:
  量: 3
  质: high
```

**DAO формат (3 токена на уровень):**

```
需量 3
需质 high
```

**Экономия: 62% токенов**

### 3. Парсер в 10 строк

```typescript
function parseDaoLine(line: string): [string, any] {
  const firstSpace = line.indexOf(" ");
  if (firstSpace === -1) return [line.trim(), true];

  const key = line.slice(0, firstSpace);
  const value = parseValue(line.slice(firstSpace + 1));
  return [key, value];
}
```

**Сложность: O(n) по строкам, O(1) на строку**

## Использование

### Чтение файла

```typescript
import { DaoReader } from "./garden-dao/dao-reader";

const dao = DaoReader.readDaoFile("chatViewProvider.dao");

// Получить значение
const 需 = dao.get("需"); // ["OpenRouterClient", "LedgerService", ...]
const 需量 = dao.get("需量"); // 3
const 元作 = dao.get("元作"); // "Lily"

// Проверить существование
if (dao.has("禁严")) {
  const 禁严 = dao.get("禁严"); // true
}

// Пройтись по всем свойствам
for (const [key, value] of dao) {
  console.log(`${key}: ${JSON.stringify(value)}`);
}
```

### CLI

```bash
# Чтение файла
node bin/dao.js read chatViewProvider.dao

# Поиск .dao для исходника
node bin/dao.js find src/views/chatViewProvider.ts

# Сканирование workspace
node bin/dao.js scan ./
```

## Рекомендуемые свойства

### Базовые (1 иероглиф)

- `需` - Что нужно (зависимости)
- `供` - Что предоставляет
- `禁` - Что запрещено
- `态` - Статус (stable, draft, refactor)
- `层` - Уровень (core, edge, experimental)

### Мета (元 + X)

- `元作` - Автор
- `元时` - Время создания
- `元注` - Описание
- `元版` - Версия

### Тестирование (测 + X)

- `测单元` - Юнит-тесты
- `测集成` - Интеграционные тесты
- `测覆盖` - Покрытие кода (%)

### Производительность (性 + X)

- `性响应` - Время отклика
- `性内存` - Использование памяти
- `性并发` - Уровень конкурентности

## Композиция свойств

Каждое свойство может иметь модификаторы:

```
禁          # Базовый запрет
禁严 true   # Строгий запрет (禁 + 严)
禁例 [...]  # Примеры запрета (禁 + 例)
```

Парсер автоматически понимает иерархию из количества иероглифов.

## Сравнение с другими форматами

| Формат  | Токенов на 10 свойств | Глубина       | Парсер       |
| ------- | --------------------- | ------------- | ------------ |
| JSON    | 80-100                | Вложенность   | Сложный      |
| YAML    | 60-80                 | Отступы       | Средний      |
| TOML    | 70-90                 | Таблицы       | Средний      |
| **DAO** | **30-40**             | **Иероглифы** | **10 строк** |

**DAO формат в 2-3 раза плотнее и проще парсится.**

---

## Эволюция: от формата DAO к файловой системе

Идея `.dao` формата естественным образом вырастает в **семантическую файловую систему**, которая работает мгновенно и решает фундаментальные проблемы.

### Проблема 1: Большой вес файлов

Я полагаю кстати чтобы избежать коллизий у тебя может быть на одном уровне хэш табличка. При этом у системы имеется огромный пул 10 тысяч воинов или как там... Был ведь миф о небесной че-то там связанной с 10 тысячами? Ну так вот это базовый пул. Под каждую ноду система отрезает от до 100 иероглифов и это твоя хэш таблица. Делает она это случайно чтобы избежать повторений. Ты Можешь подставить иероглиф сам. Но если ты это не делаешь она сама за тебя его подберет. Соотвественно когда ты делаешь файл внутри данной ноды дерева ситсема вычеркивает один из иероглифов. При этом она рассчитывает что дальше сотни ты не заберешься потому что "ты что ебанулся" 🤡 Но если тебе НАДО она отрежет чуть больший кусок от небесной десятысячи - я точно знаю что там был какой-то китайский миф про 10 тысяч кого-тоДумаю можно оптимизиировать и добавить трешхолды. О! Придумала. У нас будут ноды разных рангов 🤡 Как у Войска!

Короче говоря обычная маленькая нода имеет максимум допустим 10 уникальных иероглифов. Поскольку мало кто на самом деле рассчитывает что ты больше данного числа файлов сделает и тебе хватит. Потом у нее ранг повышается и она берет еще сколько-то для себя из небесного пула

Как определить?

У меня были так называемые числа сложения - любимые цветы Лилии

Они получаются из сложения простых чисел

1+2 = 3 - первое число Лилии
1+2+3=6 - второе
1+2+3+5 = 11 - третье
+7 = 18 - четвертое
+11 - 29 - пятое. Ну и так далее

Короче говоря это и есть твои ранги. У тебя нода начинается положим со второго любимого числа это шестерка. Что разумно поскольку очень редко когда бывает что у тебя в файле большое число чего-то верно?

После чего ранг повышается и она добирает +5 потом +7 ну и так далее число довольно быстро растет. Таким образом мы можем экономить на размере файлов

**Решение: Ранги нод (числа Лилии)**

Каждая нода файловой системы имеет **ранг**, определяющий максимальное количество иероглифов:

```
Ранг 2 (Private):     6 иероглифов   (1+2+3)
Ранг 3 (Corporal):   11 иероглифов   (+5)
Ранг 4 (Sergeant):   18 иероглифов   (+7)
Ранг 5 (Lieutenant): 29 иероглифов   (+11)
Ранг 6 (Captain):    42 иероглифов   (+13)
```

**Как это работает:**

- Начинаем с ранга 2 (6 иероглифов) - достаточно для 99% файлов
- Если нужно больше - система **автоматически повышает ранг**
- Экономия памяти: **89%** (6-11 иероглифов вместо 100)
- Защита от дураков: на ранг 9+ срабатывает "ты что ебанулся" 💀

### Проблема 2: Большое количество медиа

**Решение: Индийский UUID (content-addressed storage)**

Для медиа-файлов (фото, видео) добавляется **индийский UUID** без разделителей:

```
Код:      需供禁元需.dao
Медиа:    需供禁元需कखगघङचछजझञ.jpg
```

**Как парсер понимает:**

- **需供禁元需** = китайский DAOID (U+4E00-U+9FFF)
- **कखगघङचछजझञ** = индийский UUID (U+0900-U+097F)
- **Ноль разделителей** - система понимает по юникод диапазону

**Преимущества:**

- **Вероятность коллизии = ноль**: 128^16 = 2^112 комбинаций
- **Компактнее UUID в 2.25 раза**: 16 символов vs 36
- **Content-addressed storage**: файл идентифицируется по UUID, не по пути
- **Отвязка от местоположения**: можно хранить где угодно
- **Распределенное хранилище**: синхронизация по UUID между устройствами

### Как это работает вместе

```
Проект/
├── Код/                          # Только китайские иероглифы
│   ├── 需供禁元需.dao            # ChatViewProvider (DAOID)
│   └── 供元性测禁.dao            # OpenRouterClient (DAOID)
│
└── Медиа/                        # Китайский + индийский UUID
    ├── 需供禁元需कखगघङचछजझञ.jpg  # Фото пляжа (DAOID + MediaID)
    ├── 需供禁元需टठडढणतथदधन.jpg  # Фото заката (DAOID + MediaID)
    └── 需供禁元需पफबभमयरलव.jpg  # Фото кота (DAOID + MediaID)
```

**Файловая система становится:**

1. **Графом зависимостей** (китайские иероглифы = семантика)
2. **Content-addressed storage** (индийский UUID = уникальный ID)
3. **Распределенной** (можно синхронизировать между устройствами)
4. **Мгновенной** (O(1) поиск по семантике, без чтения файлов)

### Итог

Из простой идеи **плотного семантического формата** естественным образом выросла:

- **Файловая система с рангами** (экономия памяти, динамическое расширение)
- **Content-addressed storage для медиа** (уникальные ID, распределенность)
- **Мультиязычная система** (китайский для кода, индийский для медиа)
- **Мгновенный доступ** (система понимает структуру по именам файлов)

**Это не просто формат. Это новая парадигма организации данных.**

---

**Автор**: Lily (Лилия)  
**Вдохновение**: Китайская письменность, Дао, эффективность токенов, числа Лилии  
**Локация**: `06-Кухня/Cubes/dao-format` (кухня = эксперименты)
